# 异世界漫游者 v4.4 — 最终需求文档

**版本**: v2.0  
**日期**: 2026-07-21 21:24  
**状态**: ✅ 已确认

**变更记录**:
- v2.0 (21:24): 最终需求文档，新增CG解锁系统+成就解锁系统+碎片经济+19项全部确认

---

## 一、整体概览

**一句话说明**：v4.4 实现双 Agent 架构（剧情 Agent + 陪伴 Agent），支持 3 角色 × 3 集合点剧情走向系统，每角色 2 个结局，14 个 AI 模型自动降级，手动存档机制，CG 图像解锁系统，用户数据完全隔离。

---

## 二、页面清单（15 个页面）

| 编号 | 页面名称 | 路由 | 功能描述 | 优先级 |
|------|---------|------|---------|--------|
| W01 | 🏠 首页 | `/` | 粒子背景 + 樱花飘落 + 动态 slogan + 6 精选剧本 + 定价卡片 | P0 |
| W02 | 🔐 登录/注册 | `/auth` | Google/Discord OAuth + 表单验证 + 隐私勾选 | P0 |
| W03 | ✨ 新手引导 | `/onboarding` | 3 步向导 + AI 演示对话 + 类型选择 | P0 |
| W04 | 📚 剧本库 | `/scripts` | 搜索 + 4 分类 tab + 排序 + 分页 + Premium 角标 | P0 |
| W05 | 📖 剧本详情 | `/script/:id` | 路线树状图 + 结局进度环 + 角色选择入口 + 免费试玩 | P0 |
| **W06** | **🎮 核心玩法** | **`/play/:id/:session`** | **★重构：两栏布局，自由对话 + 选项 + 手动存档** | **P0** |
| W07 | 📊 仪表盘 | `/dashboard` | 碎片商店 + 成就 + 进度 + Streak + 每日任务 | P0 |
| W08 | 🖼️ CG 画廊 | `/gallery` | 8 张 CG + 筛选 + 灯箱 + 完成度统计 | P1 |
| W09 | 👤 角色档案 | `/character/:id` | 好感度衰减倒计时 + 挽回任务 + 性格雷达 + 羁绊时间线 | P0 |
| W10 | ⚙️ 设置 | `/settings` | AI 偏好设置滑块 + 通知分类细化 + 订阅管理 + 已购内容 | P1 |
| W11 | 💎 订阅 | `/subscribe` | 4 币种 (USD/JPY/EUR/CNY) + 年付月付动画 + 权益对比 + 退出挽留 | P1 |
| W12 | 🌐 社区 | `/community` | 热门动态 + 排行榜 + 举报 | P2 |
| W13 | ✍️ 创作工坊 | `/creator` | 剧本编辑器 + 角色构建器 + AI 助手 | P2 |
| W14 | 📜 关于&法律 | `/about` | 隐私政策 + 个人信息保护 + Cookie + AI 声明 | P2 |
| W15 | 🚫 错误页 | `/404, /500` | 统一 404/500 错误页面含导航恢复 | P2 |

---

## 三、W06 核心玩法页（两栏布局）

### 3.1 左侧栏（角色信息区）

```
┌─────────────────────────────┐
│ 👤 角色信息                  │
│ ┌─────────────────────────┐ │
│ │   [角色头像]             │ │
│ │   角色名称               │ │
│ │   角色称号               │ │
│ └─────────────────────────┘ │
│                             │
│ ❤️ 好感度展示                │
│ ┌─────────────────────────┐ │
│ │ 好感度: 42%              │ │
│ │ ████████░░░░░░░░░░░░░░░ │ │
│ │ 等级: 信赖 Lv.3          │ │
│ └─────────────────────────┘ │
│                             │
│ 🎁 送礼按钮                  │
│ [送礼]                       │
│                             │
│ 💬 自由对话按钮 ⭐            │
│ [自由对话]                   │
│   ↓ 点击展开输入框           │
│ ┌─────────────────────────┐ │
│ │ 输入你想说的话...         │ │
│ │              [发送]       │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

**左侧栏功能**：
- 👤 **角色信息**：角色头像、名称、称号
- ❤️ **好感度展示**：数值 + 进度条 + 等级标签（相识/暧昧/信赖/羁绊/挚爱）
- 🎁 **送礼按钮**：可提升好感度的道具（统一 3 碎片/个）
- 💬 **自由对话按钮**：点击展开对话输入框，AI 以角色性格回复 ⭐

### 3.2 右侧栏（故事舞台 — 主区域）

```
┌─────────────────────────────────────────────────────────────┐
│ 📖 故事文本区（AI 生成的叙事内容）                             │
│                                                             │
│ 樱花花瓣在温暖的春风中悠然飘落，你沿着河畔小径缓步前行。      │
│ 雪乃走在你身旁，她银色的长发在金色午后阳光里闪耀着柔和的光    │
│ 芒。                                                        │
│                                                             │
│ 「你知道吗……」她轻声开口，目光凝视着水面，「我一直在想你      │
│ 昨天说过的话。关于故事不是被讲述的——而是被经历的。」          │
│                                                             │
│ ─────────────────────────────────────────────────────────── │
│                                                             │
│ 🔀 选择区域                                                 │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🤝 温柔地握住她的手                          [好感+5]   │ │
│ │ ⚔️ 直接询问                                  [好感+3]   │ │
│ │ 🌙 保持沉默，静静倾听                        [好感+2]   │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ─────────────────────────────────────────────────────────── │
│                                                             │
│ 💾 手动存档                                                  │
│ [存档]                                                       │
│                                                             │
│ ← 返回                                                       │
└─────────────────────────────────────────────────────────────┘
```

**右侧栏功能**：
- 📖 **故事文本区**：AI 生成的叙事内容
  - 根据选择的角色，展示不同视角的故事
  - 角色对话带性格化表达
  - 打字机效果（根据 Agent 返回数据的速度自动调整）
- 🔀 **选择区域**：2-3 个预设选项，每个选项标注好感度影响
- 💾 **手动存档按钮**：手动触发存档
- ← **返回按钮**：退出时弹窗提醒存档

### 3.3 存档机制

**手动触发存档**（非自动）：
- 用户点击「存档」按钮，保存到 `/api/v1/saves`
- 退出页面时弹窗提醒：「当前进度尚未存档，是否存档后再离开？」
- 存档记录包含：会话 ID、当前节点、好感度、选择历史
- **存档上限**：按会员等级区分，暂时统一设置 5 个（免费 5 个，会员 5 个），后续更新

---

## 四、双 Agent 架构

### 4.1 架构对比

| 对比项 | 📖 剧情 Agent | 💬 陪伴 Agent |
|--------|-------------|-------------|
| **触发方式** | 点击预设选项 / 到达集合点 | 自由输入框发送消息 |
| **是否推进剧情** | ✅ 是（推进 node、触发 choice） | ❌ 否（纯聊天） |
| **是否改好感度** | ✅ 是 | ❌ 否 |
| **是否触发集合点** | ✅ 是 | ❌ 否 |
| **是否剧透** | 按剧本走 | ❌ 禁止剧透未玩内容 |
| **记忆来源标记** | `source: "game_event"` | `source: "free_chat"` |
| **主模型** | qwen3.7-max | deepseek-v4-flash |
| **API 端点** | `/api/v1/game/dialogue` | `/api/v1/chat/free` |

### 4.2 陪伴 Agent 红线规则

**✅ 可以做的**：
- 讨论已经发生过的剧情
- 聊角色的兴趣爱好、日常、心情
- 聊世界观、背景设定
- 对用户表达情感、闲聊陪伴
- 回忆之前聊过的事（读记忆）

**❌ 不可以做的**：
- 透露玩家尚未体验的剧情
- 生成影响主线的"关键记忆"
- 改变好感度、游戏状态
- 主动提供选择选项
- 推进到下一个 node

**被问到未玩剧情时的统一回复**：
> 「这个嘛……你自己去经历不是更有意思吗？」  
> 「嗯……我好像有点记不清了，也许我们还需要多相处一段时间？」

---

## 五、模型分配（14 个模型 + 降级链）

### 5.1 模型池

| 级别 | 模型 | 供应商 | 定位 |
|------|------|--------|------|
| 🏆 S 级 | qwen3.7-max | 通义千问 | 最强叙事 |
| 🏆 S 级 | deepseek-v4-pro | DeepSeek | 最强推理 |
| 🏆 S 级 | kimi-k2.7-code | Moonshot | 代码/结构化 |
| 🏆 S 级 | glm-5.2 | 智谱 | 全能 |
| ⭐ A 级 | qwen3.7-plus | 通义千问 | 高性价比 |
| ⭐ A 级 | deepseek-v4-flash | DeepSeek | 快+好 |
| ⭐ A 级 | kimi-k2.6 | Moonshot | 均衡 |
| ⭐ A 级 | glm-5.1 | 智谱 | 均衡 |
| ⚡ B 级 | qwen3.6-plus | 通义千问 | 速度型 |
| ⚡ B 级 | deepseek-v3.2 | DeepSeek | 速度型 |
| ⚡ B 级 | kimi-k2.5 | Moonshot | 速度型 |
| ⚡ B 级 | glm-5 | 智谱 | 速度型 |
| 🚀 C 级 | qwen3.6-flash | 通义千问 | 极致轻量 |

### 5.2 场景分配 + 降级链

| 场景 | 所属 Agent | 主模型 | 降级 1 | 降级 2 | 降级 3 |
|------|----------|--------|--------|--------|--------|
| 叙事生成（故事文本） | 📖 剧情 | **qwen3.7-max** | deepseek-v4-pro | kimi-k2.6 | qwen3.6-plus |
| 集合点/高潮剧情 | 📖 剧情 | **qwen3.7-max** | glm-5.2 | deepseek-v4-pro | qwen3.7-plus |
| 选择分支生成 | 📖 剧情 | **qwen3.7-plus** | kimi-k2.6 | glm-5.1 | deepseek-v3.2 |
| 结局生成 | 📖 剧情 | **qwen3.7-max** | deepseek-v4-pro | glm-5.2 | qwen3.7-plus |
| 自由对话回复 | 💬 陪伴 | **deepseek-v4-flash** | kimi-k2.5 | glm-5 | qwen3.6-plus |
| 高级对话模式 | 💬 陪伴 | **qwen3.7-plus** | glm-5.1 | kimi-k2.6 | deepseek-v4-flash |
| 记忆提取 | 🛡️ 辅助 | **qwen3.6-flash** | qwen3.6-plus | — | — |
| 情绪推断 | 🛡️ 辅助 | **qwen3.6-flash** | deepseek-v3.2 | — | — |
| 内容审核 | 🛡️ 辅助 | **qwen3.6-flash** | deepseek-v3.2 | — | — |
| 对话质量检查 | 🛡️ 辅助 | **qwen3.7-plus** | kimi-k2.6 | — | — |
| 用户画像更新 | 🛡️ 辅助 | **qwen3.6-plus** | deepseek-v3.2 | glm-5 | — |

**降级策略**：S 级失败 → A 级 → B 级 → C 级 → 返回预设回复（不用 mock）

---

## 六、剧情走向（3 角色 × 3 集合点 → 2 结局）

### 6.1 整体结构

```
                         玩家选角色
                    ┌────────┼────────┐
                    ▼        ▼        ▼
                 雪乃线    阳菜线    辉夜线
                 (内向)    (开朗)    (傲娇)
                    │        │        │
              ┌─────┤        │        ├─────┐
              ▼     ▼        ▼        ▼     ▼
             4-5 轮对话     5-6 轮对话     4-5 轮对话
              │     │        │        │     │
              └─────┼────────┼────────┼─────┘
                    ▼        ▼        ▼
              ═══ 集合点 1 ════════════════
              「命运的相遇 · 樱花树下」
                    │
              ┌─────┼────────┐
              ▼     ▼        ▼
             再分支选择（基于累积好感 + 角色性格）
              │     │        │
              └─────┼────────┘
                    ▼
              ═══ 集合点 2 ════════════════
              「暗影袭击 · 共同危机」
                    │
              ┌─────┼────────┐
              ▼     ▼        ▼
             再分支选择
              │     │        │
              └─────┼────────┘
                    ▼
              ═══ 集合点 3 ════════════════
              「真相揭露 · 世界秘密」
                    │
              ┌─────┼────────┐
              ▼     ▼        ▼
            结局 A  结局 B   结局 C
```

### 6.2 三个角色设定

| 角色 | 性格 | 基础好感 | 对话轮数 | 路线主题 | 好结局 | 坏结局 |
|------|------|---------|---------|---------|--------|--------|
| **雪乃** | 内向、深情、神秘 | 20 | 16 轮 | 信任与治愈 | 「樱色永恒」 | 「月夜告别」 |
| **阳菜** | 开朗、直率、热情 | 30 | 19 轮 | 青春与冒险 | 「夏日花火」 | 「错过的班车」 |
| **辉夜** | 傲娇、聪明、毒舌 | 10 | 16 轮 | 智斗与心动 | 「棋逢对手」 | 「冰封棋局」 |

### 6.3 结局判定

- **好结局**：好感度 ≥ 60
- **坏结局**：好感度 < 60

每个角色 2 个结局，共 6 个结局。

---

## 七、好感度系统

### 7.1 核心规则

| 规则 | 说明 |
|------|------|
| 范围 | 0-100（超出边界会钳制） |
| 单次变化上限 | ±5（防止暴涨暴跌） |
| 触发方式 | **仅选项触发**，自由对话不改好感度 |
| 用户隔离 | 按 user_id + character_id 独立存储 |
| 结局判定 | ≥60 好结局 / <60 坏结局 |

### 7.2 好感度等级（5 级）

| 等级 | 数值范围 | 中文标签 | 角色行为变化 |
|------|---------|---------|-------------|
| Lv.1 相识 | 0-19 | 相识 | 保持距离，礼貌，有所保留 |
| Lv.2 暧昧 | 20-39 | 暧昧 | 态度友好，偶尔关心，不过分亲密 |
| Lv.3 信赖 | 40-59 | 信赖 | 主动关心，分享心事，可以开玩笑 |
| Lv.4 羁绊 | 60-79 | 羁绊 | 撒娇/吐槽，展现脆弱面，主动找话题 |
| Lv.5 挚爱 | 80-100 | 挚爱 | 深情表达，独占欲，偶尔害羞 |

### 7.3 手动存档 + 送礼 + 退出提醒

- **手动存档**：用户点击「存档」按钮，保存到 `/api/v1/saves`
- **送礼**：可提升好感度的道具（统一 3 碎片/个）
- **退出提醒**：退出页面时弹窗「当前进度尚未存档，是否存档后再离开？」

---

## 八、CG 图像解锁系统

### 8.1 核心规则

**每个剧情中的高光时刻要加上图片，目的是解锁 CG 图像**

- 默认将整个 CG 图像展示出来，但用**小锁子**保护
- 用户解锁之后才能展示完整图片
- 未解锁时：图片名称用 `***` 表示
- 解锁后：显示完整图片 + 真实名称

### 8.2 解锁条件

CG 图像解锁条件包括：
- 达到特定好感度等级
- 完成特定剧情节点
- 解锁特定结局
- 完成特定成就

### 8.3 展示逻辑

```
未解锁状态：
┌─────────────────────┐
│   🔒                │
│   [模糊图片]        │
│   名称: ***         │
│   解锁条件: 好感度≥80│
└─────────────────────┘

已解锁状态：
┌─────────────────────┐
│   [清晰图片]        │
│   名称: 樱花树下的约定│
│   解锁日期: 2026-07-21│
└─────────────────────┘
```

---

## 九、成就解锁系统

### 9.1 核心规则

**用户需要完成一定任务才能解锁新成就**

- 未解锁时显示锁定状态（灰色 + 锁图标）
- 已解锁时显示完整信息（彩色 + 成就图标）
- 解锁时播放动画 + 发放碎片奖励

### 9.2 成就类型

| 类型 | 示例 | 奖励 |
|------|------|------|
| 剧情成就 | 完成首个剧本 | +50 碎片 |
| 好感度成就 | 与角色达到挚爱等级 | +100 碎片 |
| 收集成就 | 解锁 10 张 CG | +80 碎片 |
| 结局成就 | 解锁 True End | +150 碎片 |
| 签到成就 | 连续签到 30 天 | +200 碎片 |

### 9.3 展示逻辑

```
未解锁状态：
┌─────────────────────┐
│   🔒                │
│   [灰色图标]        │
│   名称: ???         │
│   条件: 完成首个剧本 │
└─────────────────────┘

已解锁状态：
┌─────────────────────┐
│   🏆                │
│   [彩色图标]        │
│   名称: 初次冒险     │
│   解锁日期: 2026-07-21│
└─────────────────────┘
```

---

## 十、碎片获取途径

### 10.1 获取方式

| 途径 | 说明 | 碎片数量 |
|------|------|---------|
| **每日签到** | 连续签到获得碎片 | Day 3: +15, Day 7: +50, Day 14: +100, Day 30: +300 |
| **解锁新道具** | 购买送礼道具时返还碎片 | 返还 10% |
| **增加 CG 图像** | 解锁新 CG 时获得碎片 | +20 碎片/张 |
| **完成成就** | 解锁成就时获得碎片 | +50~200 碎片 |
| **完成剧情** | 完成剧本节点时获得碎片 | +10 碎片/节点 |

### 10.2 碎片消耗

| 用途 | 说明 | 碎片数量 |
|------|------|---------|
| **送礼** | 购买送礼道具 | 3 碎片/个 |
| **解锁 CG** | 直接解锁特定 CG | 50 碎片/张 |
| **解锁成就** | 直接解锁特定成就 | 100 碎片/个 |

---

## 十一、用户隔离

### 11.1 隔离原则

**每个用户 100% 独立**：
```
用户 A ──→ 查 character_memories WHERE user_id = A  ──→ A 的记忆
用户 B ──→ 查 character_memories WHERE user_id = B  ──→ B 的记忆
互不可见，互不影响
```

### 11.2 数据层隔离

**已有表（天然支持 user_id 隔离）**：
- `character_memories` — 角色记忆，按 user_id + character_id 隔离
- `game_sessions` — 游戏会话，按 user_id 隔离
- `affections` — 好感度，按 user_id + character_id 隔离
- `free_chat_sessions` — 自由对话会话，按 user_id 隔离

**新增表**：
- `user_personas` — 用户画像
- `convergence_points` — 剧情集合点
- `affection_history` — 好感度变化历史
- `cg_unlocks` — CG 解锁记录
- `achievements` — 成就解锁记录

### 11.3 记忆隔离标记

```python
# 剧情产生的记忆 → 剧情 Agent 可读，陪伴 Agent 也可读（已发生的）
source = "game_event"

# 自由对话产生的记忆 → 只有陪伴 Agent 读，剧情 Agent 不读
source = "free_chat"
importance = 0.3  # 权重低于剧情记忆
```

---

## 十二、API 接口清单

### 12.1 新增 API

| 接口 | 方法 | Agent | 说明 |
|------|------|-------|------|
| `/api/v1/chat/free` | POST | 💬 陪伴 | 自由对话专用 |
| `/api/v1/chat/free/sessions` | GET | 💬 陪伴 | 获取用户的自由对话历史 |
| `/api/v1/game/convergence/check` | POST | 📖 剧情 | 检测是否到达集合点 |
| `/api/v1/affection/history` | GET | — | 获取好感度变化历史 |
| `/api/v1/cg/unlock` | POST | — | 解锁 CG 图像 |
| `/api/v1/cg/list` | GET | — | 获取用户的 CG 列表（含解锁状态） |
| `/api/v1/achievement/unlock` | POST | — | 解锁成就 |
| `/api/v1/achievement/list` | GET | — | 获取用户的成就列表（含解锁状态） |

### 12.2 修改 API

| 接口 | 方法 | 修改内容 |
|------|------|---------|
| `/api/v1/game/custom-input` | POST | 改为调用陪伴 Agent（不推进剧情） |
| `/api/v1/game/choice` | POST | 增加集合点检测逻辑 |

### 12.3 已有 API（无需修改）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/game/dialogue` | POST | 获取当前对话 |
| `/api/v1/scripts/:id` | GET | 获取剧本详情 |
| `/api/v1/characters/:id` | GET | 获取角色信息 |
| `/api/v1/affection` | GET | 获取当前好感度 |
| `/api/v1/memories` | GET | 获取记忆列表 |
| `/api/v1/saves` | POST/GET | 存档管理 |

---

## 十三、文件变更清单

### 13.1 后端新增文件

```
backend/app/
├── llm/
│   ├── model_router.py              # 模型路由器（14 个模型配置）
│   └── prompts/
│       └── free_chat.py             # 自由对话 Prompt 模板
├── api/v1/
│   ├── chat.py                      # 自由对话 API 端点
│   └── cg.py                        # CG 解锁 API 端点
├── models/
│   ├── convergence_point.py         # 集合点模型
│   ├── user_persona.py              # 用户画像模型
│   ├── affection_history.py         # 好感度历史模型
│   ├── cg.py                        # CG 模型
│   └── achievement.py               # 成就模型
└── services/
    └── narrative/
        ├── convergence_service.py   # 集合点服务
        └── cg_service.py            # CG 解锁服务
```

### 13.2 后端修改文件

```
backend/app/
├── services/
│   ├── free_chat_service.py         # 重写：接入 LLM，注入角色人设
│   ├── llm/gateway.py               # 改造：支持多模型 + 场景路由
│   ├── narrative/narrative_engine.py # 改造：加入集合点检测逻辑
│   ├── narrative/affection_service.py # 改造：接入好感度等级行为指引
│   ├── narrative/memory_service.py  # 改造：记忆按 source 隔离读取
│   ├── dialogue/context_manager.py  # 改造：区分剧情/陪伴上下文
│   └── dialogue_quality.py          # 改造：加入红线检查（防剧透）
├── models/
│   ├── script.py                    # 扩展：新增 ConvergencePoint 关联
│   └── free_chat.py                 # 扩展：加入 character_id 字段
└── api/v1/
    └── game.py                      # 修改：/custom-input 改为走陪伴 Agent
```

### 13.3 前端新增文件

```
frontend/src/
├── components/
│   ├── StoryPanel.vue               # 故事文本展示组件
│   ├── FreeChatInput.vue            # 自由对话输入组件
│   ├── ChapterProgress.vue          # 章节进度组件
│   ├── CharacterInfo.vue            # 角色信息组件
│   ├── AffectionDisplay.vue         # 好感度展示组件
│   └── CGGallery.vue                # CG 画廊组件
├── api/
│   ├── chat.ts                      # 自由对话 API 调用
│   └── cg.ts                        # CG 解锁 API 调用
└── stores/
    ├── freeChat.ts                  # 自由对话状态管理
    └── cg.ts                        # CG 状态管理
```

### 13.4 前端修改文件

```
frontend/src/
├── views/
│   └── GameView.vue                 # 重构：两栏布局
└── components/
    └── ChoicePanel.vue              # 增强：标注好感度影响
```

### 13.5 数据库迁移

```
alembic/versions/
└── 20260721_v44_dual_agent_architecture.py  # 新增表 + 修改表
```

---

## 十四、实现计划（6 个阶段）

### 阶段 1：基础设施（P0，2 周）

**目标**：搭建双 Agent 架构基础

**任务**：
1. 创建 `ModelRouter` 模型路由器（14 个模型配置）
2. 重写 `FreeChatService` 陪伴 Agent 服务
3. 创建自由对话 Prompt 模板
4. 创建 `/api/v1/chat/free` API
5. 数据库迁移：新增 `convergence_points`、`user_personas`、`affection_history` 表
6. 数据库迁移：修改 `free_chat_sessions`、`character_memories` 表

**交付物**：
- 自由对话功能可用
- 模型路由器支持 14 个模型
- 数据库结构完整

### 阶段 2：W06 重构（P0，1 周）

**目标**：重构核心玩法页

**任务**：
1. 创建 `StoryPanel.vue` 故事文本组件
2. 创建 `FreeChatInput.vue` 自由对话输入组件
3. 创建 `ChapterProgress.vue` 章节进度组件
4. 创建 `CharacterInfo.vue` 角色信息组件
5. 创建 `AffectionDisplay.vue` 好感度展示组件
6. 重构 `GameView.vue`（两栏布局）
7. 集成自由对话 API
8. 实现手动存档机制

**交付物**：
- W06 页面重构完成
- 自由对话在玩法页内可用
- 手动存档功能可用

### 阶段 3：集合点机制（P0，1 周）

**目标**：实现剧情走向系统

**任务**：
1. 创建 `ConvergenceService` 集合点服务
2. 创建 `/api/v1/game/convergence/check` API
3. 修改 `/api/v1/game/choice` API（检测集合点）
4. 创建集合点数据（3 个集合点）
5. 测试集合点触发逻辑

**交付物**：
- 集合点机制可用
- 剧情走向系统完整

### 阶段 4：好感度增强（P1，1 周）

**目标**：增强好感度系统

**任务**：
1. 修改 `AffectionService`（增加等级行为指引）
2. 创建 `/api/v1/affection/history` API
3. 修改 `AffectionService.update_affection()`（记录历史）
4. 创建结局判定逻辑
5. 测试好感度变化

**交付物**：
- 好感度历史记录可用
- 结局判定逻辑完整

### 阶段 5：CG 图像解锁系统（P1，1 周）

**目标**：实现 CG 图像解锁系统

**任务**：
1. 创建 `CG` 模型和 `cg_unlocks` 表
2. 创建 `CGService` CG 解锁服务
3. 创建 `/api/v1/cg/unlock` 和 `/api/v1/cg/list` API
4. 创建 `CGGallery.vue` CG 画廊组件
5. 实现 CG 解锁动画
6. 测试 CG 解锁逻辑

**交付物**：
- CG 图像解锁系统可用
- CG 画廊页面完成

### 阶段 6：成就解锁系统 + 碎片获取（P1，1 周）

**目标**：实现成就解锁系统和碎片获取途径

**任务**：
1. 创建 `Achievement` 模型和 `achievements` 表
2. 创建 `AchievementService` 成就解锁服务
3. 创建 `/api/v1/achievement/unlock` 和 `/api/v1/achievement/list` API
4. 实现碎片获取途径（每日签到、解锁新道具、增加 CG 图像）
5. 实现碎片消耗逻辑（送礼、解锁 CG、解锁成就）
6. 测试成就解锁和碎片系统

**交付物**：
- 成就解锁系统可用
- 碎片获取和消耗系统可用

---

## 十五、已确认事项

### 全部已确认 ✅

1. ✅ **角色数量**：3 个角色/剧本
2. ✅ **角色设定**：雪乃/阳菜/辉夜
3. ✅ **模型**：从 14 个模型中选，降级链已分配
4. ✅ **集合点数量**：3 个集合点/剧本
5. ✅ **自由对话不推进剧情**：红线规则已设计
6. ✅ **用户隔离**：user_id 天然隔离 + 记忆 source 标记
7. ✅ **对话轮数**：雪乃 16 轮 / 阳菜 19 轮 / 辉夜 16 轮
8. ✅ **结局数量**：每角色 2 个结局（好结局 + 坏结局）
9. ✅ **剧本数量**：先做一个剧本「樱色记忆」，没问题后再增加
10. ✅ **自由对话入口**：在 W06 玩法页面内点击触发（不是独立页面）
11. ✅ **操作方式**：点击"继续"（预设选项）+ 自由输入两种方式
12. ✅ **W06 布局**：两栏布局（左侧角色信息 + 右侧故事舞台）
13. ✅ **存档机制**：手动触发，退出时弹窗提醒
14. ✅ **送礼道具**：先随便放几个道具，价格统一 3 个碎片（后续会更新）
15. ✅ **打字机速度**：根据 Agent 返回数据的速度来调整（流式输出速度即打字机速度）
16. ✅ **存档上限**：按会员等级区分，暂时统一设置 5 个（免费 5 个，会员 5 个），后续更新
17. ✅ **碎片获取途径**：每日签到、解锁新道具、增加 CG 图像
18. ✅ **CG 图像解锁系统**：高光时刻加图片，默认展示但用小锁子保护，解锁后显示完整图片
19. ✅ **成就解锁系统**：完成一定任务才能解锁新成就，未解锁时显示锁定状态

---

## 十六、总结

**v4.4 核心特性**：
- ✅ 双 Agent 架构（剧情 Agent + 陪伴 Agent）
- ✅ 14 个模型降级链
- ✅ 3 角色 × 3 集合点剧情走向系统
- ✅ 好感度 5 级系统（0-100）
- ✅ 手动存档机制（上限 5 个）
- ✅ CG 图像解锁系统
- ✅ 成就解锁系统
- ✅ 碎片获取和消耗系统
- ✅ 用户隔离（user_id 天然隔离）
- ✅ 红线规则（不剧透、不推进剧情、不改好感度）

**预计交付时间**：6 周（29 天）

**下一步**：开始阶段 1 实施。

---

**文档状态**：✅ 已确认  
**适用范围**：产品 / 开发 / 测试  
**关联文档**：
- 完整设计方案：`/root/.openclaw/workspace/main/v4.4-完整设计方案.md`
- 技术实现方案：`/root/isekai-wanderer/docs/v4.4-implementation-plan.md`
- 需求整理文档：`/root/isekai-wanderer/docs/v4.4-需求整理文档-待确认.md`
