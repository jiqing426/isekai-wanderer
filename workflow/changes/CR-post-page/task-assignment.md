# 任务分配清单

> PM 分配时间：2026-07-24T00:45:00Z
> 基于：requirements-analysis.md

---

## 一、BE 任务清单

### 1.1 剧本大厅接口优化（P0）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| BE-D1 | 剧本表新增 `hot_value` 字段 | P0 | 0.5h | 字段类型 bigint，默认值 0 |
| BE-D2 | 修改 `/scripts` 接口支持分类筛选 | P0 | 2h | 参数：category, sortType, page, size<br>返回：categoryList, list, page, size, total, totalPage |
| BE-D3 | 实现二级稳定排序 | P0 | 1h | SQL: `ORDER BY score DESC, script_id ASC`<br>或 `ORDER BY hot_value DESC, script_id ASC` |
| BE-D4 | 实现数据库分页 | P0 | 1h | 使用 Limit Offset，禁止前端分页<br>size 限制 [10,40] |

**依赖关系**：无  
**总工作量**：4.5h

---

### 1.2 游戏内容页面接口（P0）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| BE-O1 | 游戏进度接口返回进度条数据 | P0 | 2h | 接口：`GET /game/{sessionId}/progress`<br>返回：当前章节、总章节数、进度百分比 |
| BE-O2 | 游戏状态接口返回角色信息 | P0 | 2h | 接口：`GET /game/{sessionId}/status`<br>返回：剧本名称、角色名称、好感度 |
| BE-O3 | 对话历史存储 API | P0 | 3h | 接口：`POST /game/{sessionId}/dialogue`<br>存储：用户输入、AI 回复、时间戳<br>查询：`GET /game/{sessionId}/dialogues` |

**依赖关系**：无  
**总工作量**：7h

---

### 1.3 自由对话接口修复（P0）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| BE-O14 | 修复 `/free-chat` 500 错误 | P0 | 2h | 接口返回 200，正常对话 |
| BE-O14 | 修复 `/free-chat/topics` 500 错误 | P0 | 1h | 接口返回 200，返回话题列表 |
| BE-O14 | 修复 `/free-chat/history` 500 错误 | P0 | 1h | 接口返回 200，返回历史记录 |
| BE-O15 | 实现送礼记录接口（404→200） | P0 | 2h | 接口：`GET /game/{sessionId}/gift-history`<br>返回：送礼记录列表 | ✅ 完成（待 QA 验证） |

**依赖关系**：无  
**总工作量**：6h

---

### 1.4 个人中心后端逻辑（P0）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| BE-O6 | 实现签到逻辑 | P0 | 3h | 接口：`POST /user/checkin`<br>规则：每日 1 次，连续签到奖励递增<br>返回：获得碎片数、连续天数 |
| BE-O9 | 实现游戏统计数据记录 | P0 | 3h | 接口：`GET /user/game-stats`<br>统计：游戏时长、对话次数、选择次数<br>数据库表：user_game_stats | ✅ 完成（待 QA 验证） |

**依赖关系**：无  
**总工作量**：6h

---

### 1.5 设置页面后端修复（P1）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| BE-O22 | 头像上传返回 URL 而非 base64 | P1 | 2h | 上传到 S3，返回 URL<br>接口：`POST /user/avatar` |

**依赖关系**：需确认 S3 配置  
**总工作量**：2h

---

### BE 任务汇总

| 优先级 | 任务数 | 总工作量 |
|--------|--------|----------|
| P0 | 13 | 23.5h |
| P1 | 1 | 2h |
| **总计** | **14** | **25.5h** |

---

## 二、FE 任务清单

### 2.1 登录小眼睛（P0）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-E1 | 密码框添加显示/隐藏切换按钮 | P0 | 2h | 眼睛图标在输入框右侧<br>点击切换 type="password"/"text"<br>默认密文，失焦不自动隐藏<br>线性 SVG 图标，hover 透明度变化 |

**依赖关系**：无  
**总工作量**：2h

---

### 2.2 剧本大厅页面优化（P0）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-D1 | 添加横向分类 Tab 栏 | P0 | 2h | Tab 列表：全部、校园、奇幻、悬疑、恋爱<br>切换时 page 重置为 1 |
| FE-D2 | 添加排序下拉选择器 | P0 | 1h | 选项：评分优先、热门优先<br>切换时 page 重置为 1 |
| FE-D3 | 实现滚动分页加载 | P0 | 3h | 滚动触底加载下一页<br>到达最后一页显示"没有更多剧本"<br>加载时显示 loading |
| FE-D4 | 修复同分乱序问题 | P0 | 1h | 依赖 BE 二级排序<br>验证：刷新 5 次顺序一致 |

**依赖关系**：依赖 BE-D1~D4  
**总工作量**：7h

---

### 2.3 游戏内容页面优化（P0-P1）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-O1 | 进度条数据展示 | P0 | 2h | 显示当前章节/总章节数<br>进度百分比 |
| FE-O2 | 左侧栏显示角色名称和好感度 | P0 | 2h | 显示角色头像、名称、好感度数值 |
| FE-O3 | 对话历史查看入口 | P0 | 1h | 添加"历史记录"按钮<br>弹窗显示对话列表 |
| FE-O4 | 隐藏设置按钮 | P1 | 0.5h | 移除或隐藏设置按钮 |
| FE-O5 | 添加送礼功能入口 | P1 | 2h | 添加"送礼"按钮<br>弹窗显示礼物列表<br>提交后显示送礼记录 |

**依赖关系**：依赖 BE-O1~O3, BE-O15  
**总工作量**：7.5h

---

### 2.4 个人中心优化（P0-P2）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-O6 | 签到按钮功能实现 | P0 | 2h | 点击调用签到 API<br>显示获得碎片数<br>已签到时按钮禁用 |
| FE-O7 | 已签到日期高亮 | P1 | 1h | 日历中已签到日期显示绿色 |
| FE-O8 | AI 记忆添加说明提示 | P2 | 0.5h | 标题后添加"?"图标<br>hover 显示说明文字 |
| FE-O9 | 游戏统计数据展示 | P0 | 2h | 显示游戏时长、对话次数、选择次数 |
| FE-O10 | 快速继续游玩模块 | P1 | 3h | 显示最近游戏会话<br>点击"继续游玩"跳转到游戏页 |

**依赖关系**：依赖 BE-O6, BE-O9  
**总工作量**：8.5h

---

### 2.5 移动端优化（P1）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-O11 | 删除底部导航栏 | P1 | 0.5h | 移动端只显示 Header Tab |
| FE-O12 | 游戏页 100vh 滚动修复 | P0 | 1h | `height: calc(100vh - 72px)`<br>内容可滚动 | ✅ 完成（待 QA 验证） |

**依赖关系**：无  
**总工作量**：1.5h

---

### 2.6 自由对话页面（P1）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-O13 | 页面高度调整 | P1 | 0.5h | `height: calc(100vh - 72px)` |

**依赖关系**：依赖 BE-O14  
**总工作量**：0.5h

---

### 2.7 Header 优化（P0-P1）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-O16 | 个人中心跳转修复 | P0 | 0.5h | `/profile` → `/personal-center` |
| FE-O17 | 头像显示逻辑 | P1 | 1h | 有头像显示头像，无头像显示默认图标 |
| FE-O18 | 登录状态校验 | P1 | 1h | 未登录点击 Tab 跳转登录页<br>已登录隐藏登录/注册按钮 |
| FE-O26 | Header 未登录展示所有 Tab | P1 | 1.5h | 未登录状态下展示所有 tab（剧本大厅、角色馆、收藏、碎片、订阅、社区）<br>点击需登录的 tab 跳转登录页（复用 FE-O18）<br>已登录状态行为不变 | ✅ 已完成 |
| FE-O26-fix | 语言切换右侧登录注册按钮修复 | P1 | 0.5h | 语言切换区域右侧显示登录/注册按钮<br>未登录显示"登录/注册"<br>已登录显示用户头像或用户名 | ✅ 已完成 QA 6/6 |
| FE-O27 | 全局 .page-bg min-height 统一 | P1 | 1h | 修改 global.css 为 `min-height: calc(100vh - 72px)`<br>清理 GameView/FreeChatView 局部覆盖<br>认证页面保持 `min-height: 100vh`<br>页面布局无异常 | ✅ 完成（待 QA 验证） |

**依赖关系**：无  
**总工作量**：4h

---

### 2.8 设置页面优化（P1-P2）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-O19 | 头像上传限制提示修改 | P1 | 0.5h | 提示改为"不能超过 5MB" |
| FE-O20 | 播放偏好进度条保存 | P1 | 1h | 滑动时不保存，点击"保存"才提交 |
| FE-O21 | 个人资料布局调整 | P2 | 1h | 邮箱和昵称一行展示 |
| FE-O22 | 头像上传逻辑修改 | P1 | 1h | 上传后显示 S3 URL，不再处理 base64 |

**依赖关系**：依赖 BE-O22  
**总工作量**：3.5h

---

### 2.9 其他页面修复（P2）

| 任务ID | 任务描述 | 优先级 | 工作量 | 验收标准 |
|--------|----------|--------|--------|----------|
| FE-O23 | 碎片商城成就奖励跳转修复 | P2 | 0.5h | 跳转到收藏馆成就页面 |
| FE-O24 | 收藏馆成就墙 JSON 数据修复 | P1 | 1h | 不显示原始 JSON 数据 |
| FE-O25 | 登录错误提示文案修改 | P2 | 0.5h | "登录失败，请重试" → "密码或邮箱错误" |

**依赖关系**：无  
**总工作量**：2h

---

### FE 任务汇总

| 优先级 | 任务数 | 总工作量 |
|--------|--------|----------|
| P0 | 12 | 31h |
| P1 | 13 | 15.5h |
| P2 | 5 | 4h |
| **总计** | **30** | **50.5h** |

---

## 三、任务依赖关系图

```
BE 任务                          FE 任务
─────────────────────────────────────────────────
BE-D1~D4 (剧本大厅接口)    →    FE-D1~D4 (剧本大厅页面)
BE-O1~O3 (游戏内容接口)    →    FE-O1~O5 (游戏内容页面)
BE-O6 (签到逻辑)           →    FE-O6 (签到按钮)
BE-O9 (游戏统计)           →    FE-O9 (游戏统计展示)
BE-O14 (自由对话修复)      →    FE-O13 (自由对话页面)
BE-O15 (送礼记录)          →    FE-O5 (送礼功能)
BE-O22 (头像上传 S3)       →    FE-O22 (头像上传逻辑)

独立任务（无依赖）：
BE: 无
FE: FE-E1, FE-O10~O12, FE-O16~O21, FE-O23~O26
```

---

## 四、执行建议

### 阶段一：P0 任务（3 天）

**Day 1**：
- BE: BE-D1~D4 (剧本大厅接口) + BE-O14 (自由对话修复)
- FE: FE-E1 (登录小眼睛) + FE-O16 (Header 跳转修复)

**Day 2**：
- BE: BE-O1~O3 (游戏内容接口) + BE-O6 (签到逻辑)
- FE: FE-D1~D4 (剧本大厅页面) + FE-O1~O3 (游戏内容页面)

**Day 3**：
- BE: BE-O9 (游戏统计) + BE-O15 (送礼记录)
- FE: FE-O6 (签到按钮) + FE-O9 (游戏统计) + FE-O12 (移动端滚动修复)

### 阶段二：P1 任务（2 天）

**Day 4**：
- BE: BE-O22 (头像上传 S3)
- FE: FE-O5 (送礼功能) + FE-O7 (签到高亮) + FE-O10 (快速继续) + FE-O11 (删除底部导航)

**Day 5**：
- FE: FE-O13~O21, FE-O26 (自由对话、Header、设置页面优化)

### 阶段三：P2 任务（1 天）

**Day 6**：
- FE: FE-O8, FE-O21, FE-O23~O25 (说明提示、布局、跳转、文案)

---

## 五、验收测试点

### 剧本大厅
- [ ] 多次刷新，同分剧本顺序固定
- [ ] 切换分类，列表正确过滤
- [ ] 切换排序，列表按权重重新排序
- [ ] 滚动下拉正常加载分页
- [ ] 到达最后一页停止加载

### 登录小眼睛
- [ ] 密码框有眼睛图标
- [ ] 点击切换明文/密文
- [ ] 默认密文，失焦不自动隐藏
- [ ] 图标样式符合参考页面

### 游戏内容页面
- [ ] 进度条显示正常
- [ ] 左侧栏显示角色信息
- [ ] 对话历史可查看
- [ ] 送礼功能可用

### 个人中心
- [ ] 签到按钮可点击
- [ ] 已签到日期高亮
- [ ] 游戏统计显示正确
- [ ] 快速继续可用

### 移动端
- [ ] 无底部导航栏
- [ ] 游戏页面可滚动

### 自由对话
- [ ] 3 个接口返回 200
- [ ] 页面高度正确

### Header
- [ ] 个人中心跳转正确
- [ ] 头像显示正确
- [ ] 登录状态校验正确

### 设置页面
- [ ] 头像上传限制 5MB
- [ ] 播放偏好可保存
- [ ] 头像上传返回 URL

### 其他
- [ ] 碎片商城成就跳转正确
- [ ] 收藏馆无 JSON 数据
- [ ] 登录错误提示正确
