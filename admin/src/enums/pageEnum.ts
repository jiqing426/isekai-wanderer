export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // basic home path — redirect to isekai dashboard after login
  BASE_HOME = '/isekai/dashboard',
  // error page path
  ERROR_PAGE = '/exception',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
}
export const PageWrapperFixedHeightKey = 'PageWrapperFixedHeight';
