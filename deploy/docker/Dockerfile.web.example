FROM runtime-base-image AS runtime

WORKDIR /app

# Copy built frontend or admin artifact according to the selected stack.
# COPY ./dist ./dist

ENV NODE_ENV=production
EXPOSE 3000

CMD ["sh", "-c", "echo 'Replace with web start command' && sleep infinity"]
