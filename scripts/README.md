# Scripts

统一工程脚本入口目录。脚本应作为项目常用操作的稳定入口，避免团队成员记忆不同模块的零散命令。

## 约定

- `dev.sh`：本地开发启动入口。
- `build.sh`：构建入口。
- `test.sh`：测试入口。
- `deploy.sh`：部署入口模板。

脚本必须失败即退出、输出清晰、避免隐式修改生产资源。涉及部署、数据迁移或破坏性操作时，必须要求明确环境和确认变量。
