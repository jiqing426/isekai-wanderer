# 异世界漫游者 v4.4 — 技术实现方案

**版本**: v1.1  
**日期**: 2026-07-21 20:48  
**状态**: ✅ 已确认

**变更记录**:
- v1.1 (20:48): 实现方案，含两栏布局+手动存档+Vue组件结构+数据库变更+API清单

---

## 一、页面整合分析（w01-w15）

### 1.1 页面清单与对应关系

| 原始页面 | 功能描述 | 现有项目对应 | 操作 | 优先级 |
|---------|---------|-------------|------|--------|
| **W01 首页** | 粒子背景+樱花飘落+动态slogan+6精选剧本+定价卡片 | `LandingView.vue` | **保留+增强** | P0 |
| **W02 登录/注册** | Google/Discord OAuth+表单验证+隐私勾选 | `LoginView.vue`, `RegisterView.vue` | **保留+增强** | P0 |
| **W03 新手引导** | 3步向导+AI演示对话+类型选择 | `OnboardingView.vue` | **保留+增强** | P0 |
| **W04 剧本库** | 搜索+4分类tab+排序+分页+Premium角标 | `DiscoverView.vue` | **保留+增强** | P0 |
| **W05 剧本详情** | 路线树状图+结局进度环+角色选择入口+免费试玩 | `ScriptDetailView.vue` | **保留+增强** | P0 |
| **W06 核心玩法** | ★重构重点：删game tab，保留自由对话+选项 | `GameView.vue` | **★重构** | P0 |
| **W07 仪表盘** | 碎片商店+成就+进度+Streak+每日任务 | `HomeView.vue` | **保留+增强** | P0 |
| **W08 CG画廊** | 8张CG+筛选+灯箱+完成度统计 | `GalleryView.vue` | **保留** | P1 |
| **W09 角色档案** | 好感度衰减倒计时+挽回任务+性格雷达+羁绊时间线 | `CharacterDetailView.vue` | **保留+增强** | P0 |
| **W10 设置** | AI偏好设置滑块+通知分类细化+订阅管理+已购内容 | `SettingsView.vue` | **保留+增强** | P1 |
| **W11 订阅** | 4币种(USD/JPY/EUR/CNY)+年付月付动画+权益对比+退出挽留 | `SubscriptionView.vue` | **保留+增强** | P1 |
| **W12 社区** | 热门动态+排行榜+举报 | `CommunityView.vue` | **保留** | P2 |
| **W13 创作工坊** | 剧本编辑器+角色构建器+AI助手 | 无 | **新增** | P2 |
| **W14 关于&法律** | 隐私政策+个人信息保护+Cookie+AI声明 | 无 | **新增** | P2 |
| **W15 错误页** | 统一404/500错误页面含导航恢复 | `NotFoundView.vue` | **保留+增强** | P2 |

### 1.2 保留现有 Header + 语言切换器

**现有 Header 结构**（`AppHeader.vue`）：
```
🗡️ 异世界漫游者 | 📚 剧本库 | 📊 仪表盘 | 🖼️ CG画廊 | 🌐 社区 | 💎 订阅 || 🌙主题 | 🔐登录
```

**增强方案**：
```
🗡️ 异世界漫游者 | 📚 剧本库 | 📊 仪表盘 | 🖼️ CG画廊 | 🌐 社区 | 💎 订阅 || 🌐语言 | 🌙主题 | 🔐登录
```

**新增语言切换器**：
```vue
<!-- AppHeader.vue -->
<n-dropdown :options="langOptions" @select="handleLangSelect">
  <n-button quaternary size="small" class="tool-btn">
    <span>{{ currentLangIcon }}</span>
  </n-button>
</n-dropdown>

<script setup>
const langOptions = [
  { label: '🇨🇳 中文', key: 'zh-CN' },
  { label: '🇺🇸 English', key: 'en' },
  { label: '🇯🇵 日本語', key: 'ja' },
];

function handleLangSelect(lang: string) {
  locale.value = lang;
  localStorage.setItem('locale', lang);
}
</script>
```

---

## 二、W06 核心玩法页重构方案（★重点）

### 2.1 现有 W06 模块分析

**现有 GameView.vue 结构**（三栏布局）：
```
┌─────────────────────────────────────────────────────────────┐
│ Header: 剧本名 | 🔊语音 | 🗺️路线图 | 💬自由对话 | ←返回      │
├──────────┬──────────────────────────────────┬───────────────┤
│ 左栏     │ 中栏：场景舞台                    │ 右栏          │
│ 📜对话   │ ┌────────────────────────────┐   │ 🔀选择面板    │
│ 历史     │ │ 场景背景图                  │   │               │
│ (180px)  │ │ + 场景标签                  │   │ 💬自由输入    │
│          │ │ + 好感度浮动                │   │               │
│          │ │ + DialogueBox(旁白)         │   │ 🏆结局卡片    │
│          │ └────────────────────────────┘   │               │
└──────────┴──────────────────────────────────┴───────────────┘
```

**原始 HTML W06 模块**（需要删除的部分）：
- ❌ 场景背景切换（樱花/夜晚/黄昏）— 改为自动根据剧情切换
- ❌ 角色立绘+表情切换按钮 — 删除，改为AI自动生成
- ❌ 快捷操作栏（自动播放/跳过/存档）— 删除，改为自动
- ❌ 对话历史侧栏（独立面板）— 保留但改为左栏内嵌
- ❌ 记忆回溯弹窗 — 删除，改为在设置中查看
- ❌ 实时翻译按钮 — 删除，改为设置中开启
- ❌ AI语音播放按钮 — 删除，改为设置中开启

**保留的模块**：
- ✅ 故事文本区（AI生成的叙事内容）
- ✅ 选择分支（AI根据角色性格+当前剧情生成）
- ✅ 自由对话输入框（⭐核心保留）
- ✅ 好感度显示（左侧栏展示）
- ✅ 章节进度信息（顶部显示）
- ✅ **手动存档**（非自动存档，手动触发 + 退出页面提醒）

### 2.2 W06 重构后结构（★两栏布局）

```
┌───────────────────────────────────────────────────────────────────┐
│ Header: 第X章 · [集合点名] · 进度 XX%                  | ←返回   │
├────────────────┬──────────────────────────────────────────────────┤
│    左侧栏       │              右侧栏：故事舞台                     │
│                │                                                  │
│ 👤 角色信息     │  📖 故事文本区（AI生成的叙事内容）                   │
│  角色头像       │  - 根据选择的角色，展示不同视角的故事                 │
│  角色名         │  - 角色对话带性格化表达                             │
│  角色称号       │  - 打字机效果（可调节速度）                         │
│                │                                                  │
│ ❤️ 好感度展示   │                                                  │
│  数值: 42%     │                                                  │
│  ━━━━━━░░░░░  │                                                  │
│  信赖 Lv.3    │                                                  │
│                │                                                  │
│ 🎁 送礼按钮     │                                                  │
│  (好感度道具)   │                                                  │
│                │                                                  │
│ 💬 自由对话 ⭐  │                                                  │
│  (点击展开输入) │                                                  │
│                │                                                  │
│ 💾 手动存档     │                                                  │
│  (退出时提醒)   │                                                  │
└────────────────┴──────────────────────────────────────────────────┘
```

**左侧栏（角色信息 + 操作入口）：**
- 👤 角色头像 / 名称 / 称号
- ❤️ 好感度展示（数值 + 进度条 + 等级标签：相识/暧昧/信赖/羁绊/挚爱）
- 🎁 送礼按钮（可提升好感度的道具）
- 💬 自由对话按钮（点击展开对话输入框，AI以角色性格回复）⭐

**右侧栏（故事舞台 — 主区域）：**
- 📖 故事文本区（AI生成的叙事内容）
  - 根据选择的角色，展示不同视角的故事
  - 角色对话带性格化表达
  - 打字机效果（可调节速度）

**存档机制（手动触发）：**
- ~~自动存档~~ → **手动触发存档**
- 退出页面时弹窗提醒：「当前进度尚未存档，是否存档后再离开？」
- 存档记录保存到 `/api/v1/saves`
```

### 2.3 Vue 组件结构设计

**新增组件**：
```
src/components/
├── StoryPanel.vue          # 故事文本展示组件（打字机效果）
├── ChoicePanel.vue         # 选择分支组件（故事文本下方的选项卡片）
├── FreeChatInput.vue       # 自由对话输入组件（左侧栏触发）
├── CharacterInfo.vue       # 角色信息组件（左侧栏：头像/名称/称号）
├── AffectionDisplay.vue    # 好感度展示组件（左侧栏：数值+进度条+等级）
└── ChapterProgress.vue     # 章节进度组件（顶部）
```

**GameView.vue 重构后结构（两栏布局）**：
```vue
<template>
  <div class="game-page">
    <!-- 顶部：章节进度 -->
    <ChapterProgress 
      :chapter="currentChapter"
      :convergence-point="currentConvergencePoint"
      :progress="chapterProgress"
    />

    <!-- 主内容区：两栏布局 -->
    <div class="game-layout">
      <!-- 左侧栏：角色信息 + 操作入口 -->
      <aside class="left-sidebar">
        <!-- 角色信息 -->
        <CharacterInfo
          :character-id="game.currentDialogue?.character_id"
          :character-name="characterDisplayName"
          :character-title="characterTitle"
        />

        <!-- 好感度展示 -->
        <AffectionDisplay
          :character-id="game.currentDialogue?.character_id"
          :value="currentAffection"
          :level="affectionLevel"
        />

        <!-- 送礼按钮 -->
        <n-button @click="openGiftModal" class="action-btn">
          🎁 送礼
        </n-button>

        <!-- 自由对话按钮 -->
        <n-button @click="toggleFreeChat" class="action-btn">
          💬 自由对话
        </n-button>

        <!-- 自由对话输入框（点击按钮后展开） -->
        <FreeChatInput
          v-if="showFreeChat"
          :character-id="game.currentDialogue?.character_id"
          :character-name="characterDisplayName"
          :disabled="game.loading"
          @send="handleFreeChat"
          @close="showFreeChat = false"
        />

        <!-- 手动存档按钮 -->
        <n-button @click="handleManualSave" class="action-btn">
          💾 手动存档
        </n-button>
      </aside>

      <!-- 右侧栏：故事舞台（主区域） -->
      <main class="right-story-stage">
        <!-- 故事文本区 -->
        <StoryPanel 
          :text="game.currentDialogue?.text"
          :character-name="characterDisplayName"
          :emotion="game.currentDialogue?.emotion"
          :speed="typewriterSpeed"
          @complete="onStoryComplete"
        />

        <!-- 选择区域（故事文本下方的选项卡片） -->
        <ChoicePanel 
          v-if="game.hasChoices"
          :choices="choiceOptions"
          @select="handleChoice"
        />
      </main>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue';
import { useGameStore } from '@/stores/game';
import { useAffectionStore } from '@/stores/affection';
import { chatApi } from '@/api/chat';
import { saveApi } from '@/api/saves';

import StoryPanel from '@/components/StoryPanel.vue';
import ChoicePanel from '@/components/ChoicePanel.vue';
import FreeChatInput from '@/components/FreeChatInput.vue';
import CharacterInfo from '@/components/CharacterInfo.vue';
import AffectionDisplay from '@/components/AffectionDisplay.vue';
import ChapterProgress from '@/components/ChapterProgress.vue';

const game = useGameStore();
const affectionStore = useAffectionStore();

// 自由对话状态
const showFreeChat = ref(false);

// 切换自由对话输入框
function toggleFreeChat() {
  showFreeChat.value = !showFreeChat.value;
}

// 自由对话处理
async function handleFreeChat(message: string) {
  const characterId = game.currentDialogue?.character_id;
  if (!characterId) return;

  const response = await chatApi.sendFreeChat({
    character_id: characterId,
    message: message,
    script_id: game.currentScript?.id,
  });

  // 显示AI回复（不推进剧情）
  showFreeChatReply(response.reply);
}

// 手动存档
async function handleManualSave() {
  if (!game.currentSession) return;
  
  await saveApi.createSave({
    session_id: game.currentSession.id,
    script_id: game.currentScript?.id,
    character_id: game.currentDialogue?.character_id,
    current_node_id: game.currentNode?.id,
    affection_value: currentAffection.value,
    choice_history: game.choiceHistory,
  });
  
  message.success('存档成功');
}

// 退出页面提醒
onBeforeRouteLeave((to, from, next) => {
  if (game.currentSession && !game.isSaved) {
    const confirmed = window.confirm('当前进度尚未存档，是否存档后再离开？');
    if (confirmed) {
      handleManualSave();
    }
  }
  next();
});
</script>

<style scoped>
.game-layout {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 24px;
  height: calc(100vh - 120px);
}

.left-sidebar {
  display: flex;
  flex-direction: column;
  gap: 16px;
  padding: 16px;
  background: var(--glass-bg);
  border: 1px solid var(--border-color);
  border-radius: 16px;
  overflow-y: auto;
}

.right-story-stage {
  display: flex;
  flex-direction: column;
  gap: 24px;
  padding: 24px;
  background: var(--bg-primary);
  border-radius: 16px;
  overflow-y: auto;
}

.action-btn {
  width: 100%;
  justify-content: center;
}
</style>
```

### 2.4 自由对话嵌入方式

**触发方式**：
- 左侧栏的「💬 自由对话」按钮触发
- 点击按钮后展开输入框
- 用户可以随时输入，不影响主线剧情
- AI回复显示在输入框上方（气泡形式）

**数据流**：
```
用户输入 → FreeChatInput.vue → chatApi.sendFreeChat() 
→ 后端 /api/v1/chat/free → 陪伴Agent(deepseek-v4-flash) 
→ 返回回复 → 显示在页面（不更新game store）
```

**记忆标记**：
```python
# 自由对话产生的记忆
memory = {
  "user_id": user_id,
  "character_id": character_id,
  "content": message,
  "source": "free_chat",  # 标记为自由对话
  "importance": 0.3,       # 权重低于剧情记忆
  "emotion": emotion,
}
```

---

## 三、后端实现方案

### 3.1 需要新增/修改的文件清单

#### 新增文件
```
backend/app/
├── llm/
│   ├── model_router.py              # 模型路由器（新增）
│   └── prompts/
│       ├── character.py             # 角色人设模板（增强）
│       └── free_chat.py             # 自由对话Prompt（新增）
├── api/v1/
│   └── chat.py                      # 自由对话API端点（新增）
├── models/
│   ├── convergence_point.py         # 集合点模型（新增）
│   └── user_persona.py              # 用户画像模型（新增）
└── services/
    └── narrative/
        └── convergence_service.py   # 集合点服务（新增）
```

#### 修改文件
```
backend/app/
├── services/
│   ├── free_chat_service.py         # 重写：接入LLM，注入角色人设
│   ├── llm/gateway.py               # 改造：支持多模型+场景路由
│   ├── narrative/narrative_engine.py # 改造：加入集合点检测逻辑
│   ├── narrative/affection_service.py # 改造：接入好感度等级行为指引
│   ├── narrative/memory_service.py  # 改造：记忆按source隔离读取
│   ├── dialogue/context_manager.py  # 改造：区分剧情/陪伴上下文
│   └── dialogue_quality.py          # 改造：加入红线检查（防剧透）
├── models/
│   ├── script.py                    # 扩展：新增ConvergencePoint关联
│   └── free_chat.py                 # 扩展：加入character_id字段
└── api/v1/
    └── game.py                      # 修改：/custom-input改为走陪伴Agent
```

### 3.2 模型路由器设计

**文件**: `backend/app/llm/model_router.py`

```python
"""
ModelRouter — 根据场景自动选择模型+降级链
"""

from enum import Enum
from typing import List, Optional
from dataclasses import dataclass


class ScenarioType(str, Enum):
    """场景类型"""
    NARRATIVE = "narrative"              # 叙事生成
    CONVERGENCE = "convergence"          # 集合点/高潮剧情
    CHOICE_GENERATION = "choice_gen"     # 选择分支生成
    ENDING = "ending"                    # 结局生成
    FREE_CHAT = "free_chat"              # 自由对话
    FREE_CHAT_ADVANCED = "free_chat_adv" # 高级对话模式
    MEMORY_EXTRACTION = "memory_ext"     # 记忆提取
    EMOTION_INFERENCE = "emotion_inf"    # 情绪推断
    CONTENT_MODERATION = "content_mod"   # 内容审核
    DIALOGUE_QUALITY = "dialogue_qual"   # 对话质量检查
    USER_PERSONA_UPDATE = "persona_upd"  # 用户画像更新


@dataclass
class ModelConfig:
    """模型配置"""
    name: str
    provider: str
    tier: str  # S/A/B/C
    fallback_chain: List[str]


# 14个模型池
MODEL_POOL = {
    # S级
    "qwen3.7-max": ModelConfig("qwen3.7-max", "qwen", "S", ["deepseek-v4-pro", "kimi-k2.6", "qwen3.6-plus"]),
    "deepseek-v4-pro": ModelConfig("deepseek-v4-pro", "deepseek", "S", ["qwen3.7-max", "kimi-k2.6", "qwen3.6-plus"]),
    "kimi-k2.7-code": ModelConfig("kimi-k2.7-code", "moonshot", "S", ["glm-5.2", "qwen3.7-max", "qwen3.6-plus"]),
    "glm-5.2": ModelConfig("glm-5.2", "zhipu", "S", ["deepseek-v4-pro", "qwen3.7-max", "qwen3.6-plus"]),
    
    # A级
    "qwen3.7-plus": ModelConfig("qwen3.7-plus", "qwen", "A", ["kimi-k2.6", "glm-5.1", "deepseek-v3.2"]),
    "deepseek-v4-flash": ModelConfig("deepseek-v4-flash", "deepseek", "A", ["kimi-k2.5", "glm-5", "qwen3.6-plus"]),
    "kimi-k2.6": ModelConfig("kimi-k2.6", "moonshot", "A", ["glm-5.1", "qwen3.7-plus", "deepseek-v3.2"]),
    "glm-5.1": ModelConfig("glm-5.1", "zhipu", "A", ["kimi-k2.6", "qwen3.7-plus", "deepseek-v3.2"]),
    
    # B级
    "qwen3.6-plus": ModelConfig("qwen3.6-plus", "qwen", "B", ["deepseek-v3.2", "glm-5", "kimi-k2.5"]),
    "deepseek-v3.2": ModelConfig("deepseek-v3.2", "deepseek", "B", ["qwen3.6-plus", "kimi-k2.5", "glm-5"]),
    "kimi-k2.5": ModelConfig("kimi-k2.5", "moonshot", "B", ["glm-5", "qwen3.6-plus", "deepseek-v3.2"]),
    "glm-5": ModelConfig("glm-5", "zhipu", "B", ["kimi-k2.5", "qwen3.6-plus", "deepseek-v3.2"]),
    
    # C级
    "qwen3.6-flash": ModelConfig("qwen3.6-flash", "qwen", "C", []),
}

# 场景→模型映射
SCENARIO_MODEL_MAP = {
    ScenarioType.NARRATIVE: "qwen3.7-max",
    ScenarioType.CONVERGENCE: "qwen3.7-max",
    ScenarioType.CHOICE_GENERATION: "qwen3.7-plus",
    ScenarioType.ENDING: "qwen3.7-max",
    ScenarioType.FREE_CHAT: "deepseek-v4-flash",
    ScenarioType.FREE_CHAT_ADVANCED: "qwen3.7-plus",
    ScenarioType.MEMORY_EXTRACTION: "qwen3.6-flash",
    ScenarioType.EMOTION_INFERENCE: "qwen3.6-flash",
    ScenarioType.CONTENT_MODERATION: "qwen3.6-flash",
    ScenarioType.DIALOGUE_QUALITY: "qwen3.7-plus",
    ScenarioType.USER_PERSONA_UPDATE: "qwen3.6-plus",
}


class ModelRouter:
    """模型路由器"""
    
    def get_model_for_scenario(self, scenario: ScenarioType) -> ModelConfig:
        """根据场景获取主模型"""
        model_name = SCENARIO_MODEL_MAP.get(scenario, "qwen3.6-plus")
        return MODEL_POOL[model_name]
    
    def get_fallback_chain(self, scenario: ScenarioType) -> List[ModelConfig]:
        """获取降级链"""
        primary = self.get_model_for_scenario(scenario)
        fallback_models = [MODEL_POOL[name] for name in primary.fallback_chain]
        return [primary] + fallback_models
    
    async def call_with_fallback(self, scenario: ScenarioType, messages: List[dict], **kwargs):
        """带降级的模型调用"""
        chain = self.get_fallback_chain(scenario)
        
        for model in chain:
            try:
                # 调用模型
                response = await self._call_model(model, messages, **kwargs)
                return response
            except Exception as e:
                # 记录错误，尝试下一个模型
                print(f"Model {model.name} failed: {e}, trying fallback...")
                continue
        
        # 所有模型都失败，返回预设回复
        return self._get_fallback_response(scenario)
    
    async def _call_model(self, model: ModelConfig, messages: List[dict], **kwargs):
        """调用具体模型（通过LLM Gateway）"""
        from app.services.llm.gateway import llm_gateway
        return await llm_gateway.call(model.name, messages, **kwargs)
    
    def _get_fallback_response(self, scenario: ScenarioType):
        """预设回复（所有模型都失败时）"""
        fallback_responses = {
            ScenarioType.FREE_CHAT: "（微微侧头）嗯……我好像有点记不清了，也许我们还需要多相处一段时间？",
            ScenarioType.NARRATIVE: "故事还在继续，请稍等片刻……",
        }
        return {"text": fallback_responses.get(scenario, "请稍后再试。")}


# 全局单例
model_router = ModelRouter()
```

### 3.3 陪伴Agent重写方案

**文件**: `backend/app/services/free_chat_service.py`

```python
"""
FreeChatService — 自由对话服务（陪伴Agent）

核心原则：
- 不推进剧情
- 不改好感度
- 不剧透未玩内容
- 注入角色人设
- 用户隔离
"""

import uuid
from datetime import datetime, timezone
from typing import Any, List, Optional

from app.llm.model_router import model_router, ScenarioType
from app.llm.prompts.free_chat import build_free_chat_prompt
from app.services.narrative.memory_service import memory_service
from app.models.free_chat import FreeChatSession, FreeChatMessage


class FreeChatService:
    """自由对话服务（陪伴Agent）"""
    
    async def send_message(
        self,
        user_id: str,
        character_id: str,
        message: str,
        script_id: Optional[str] = None,
        session_id: Optional[str] = None,
    ) -> dict[str, Any]:
        """
        发送自由对话消息
        
        Args:
            user_id: 用户ID
            character_id: 角色ID
            message: 用户消息
            script_id: 剧本ID（可选，提供剧情上下文）
            session_id: 会话ID（可选，复用已有会话）
        
        Returns:
            {
                "reply": "AI回复文本",
                "emotion": "情绪标签",
                "character_id": "角色ID",
                "session_id": "会话ID",
            }
        """
        # 1. 获取或创建会话
        if not session_id:
            session_id = str(uuid.uuid4())
            session = FreeChatSession(
                id=session_id,
                user_id=user_id,
                character_id=character_id,
                script_id=script_id,
            )
            # 保存到数据库
            await self._save_session(session)
        
        # 2. 获取角色人设
        character_persona = await self._get_character_persona(character_id)
        
        # 3. 获取当前好感度
        affection_value = await self._get_affection(user_id, character_id)
        
        # 4. 获取最近对话历史（最近5条）
        recent_messages = await self._get_recent_messages(session_id, limit=5)
        
        # 5. 获取相关记忆（source="free_chat" 或 "game_event"）
        memories = await memory_service.get_memories(
            user_id=user_id,
            character_id=character_id,
            source_filter=["free_chat", "game_event"],
            limit=10,
        )
        
        # 6. 构建Prompt
        system_prompt = build_free_chat_prompt(
            character_name=character_persona["name"],
            character_persona=character_persona,
            affection_value=affection_value,
            recent_messages=recent_messages,
            memories=memories,
        )
        
        # 7. 调用陪伴Agent（deepseek-v4-flash）
        messages = [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": message},
        ]
        
        response = await model_router.call_with_fallback(
            scenario=ScenarioType.FREE_CHAT,
            messages=messages,
            max_tokens=300,
            temperature=0.7,
        )
        
        reply_text = response.get("text", "")
        emotion = self._detect_emotion(reply_text)
        
        # 8. 保存对话到数据库
        await self._save_message(session_id, "user", message)
        await self._save_message(session_id, "assistant", reply_text)
        
        # 9. 异步提取记忆（后台任务）
        await memory_service.extract_memory_async(
            user_id=user_id,
            character_id=character_id,
            content=message,
            source="free_chat",
            importance=0.3,
        )
        
        return {
            "reply": reply_text,
            "emotion": emotion,
            "character_id": character_id,
            "session_id": session_id,
        }
    
    async def _get_character_persona(self, character_id: str) -> dict:
        """获取角色人设（从数据库或配置文件）"""
        # TODO: 从数据库查询角色人设
        # 这里先用示例数据
        personas = {
            "yukino": {
                "name": "雪乃",
                "title": "神秘的银发少女",
                "traits": "内向、深情、神秘、温柔",
                "likes": "安静、星空、诗歌、彼岸花",
                "dislikes": "嘈杂、谎言、人群",
                "speak_style": "温柔含蓄，常用「……」，偶尔引用诗句",
                "example_sentences": [
                    "「你知道吗……」她轻声开口，「我一直在想你昨天说过的话。」",
                    "「和你在一起的每一刻，都像是被星光笼罩。」",
                ],
            },
            "hina": {
                "name": "阳菜",
                "title": "阳光活泼的邻家女孩",
                "traits": "开朗、直率、热情、爱冒险",
                "likes": "冒险、美食、祭典、阳光",
                "dislikes": "犹豫、孤独、阴天",
                "speak_style": "元气满满，爱用感叹号，偶尔撒娇",
                "example_sentences": [
                    "「快快快！听说河那边新开了一家团子店！」",
                    "「走嘛走嘛，冒险之前先填饱肚子！这可是我的原则！」",
                ],
            },
            "kaguya": {
                "name": "辉夜",
                "title": "高冷的学生会长",
                "traits": "傲娇、聪明、外冷内热、毒舌",
                "likes": "读书、茶道、棋类、独处",
                "dislikes": "懒散、无理取闹、笨蛋",
                "speak_style": "冷静分析，偶尔毒舌，关键时刻温柔",
                "example_sentences": [
                    "「……你迟到了三分钟。」",
                    "「不是在意这种事，只是……精确是一种美德。算了，坐下吧。茶还温着。」",
                ],
            },
        }
        return personas.get(character_id, personas["yukino"])
    
    async def _get_affection(self, user_id: str, character_id: str) -> int:
        """获取当前好感度"""
        from app.services.narrative.affection_service import affection_service
        aff = await affection_service.get_affection(user_id, character_id)
        return aff.value if aff else 0
    
    async def _get_recent_messages(self, session_id: str, limit: int = 5) -> List[dict]:
        """获取最近对话历史"""
        # TODO: 从数据库查询
        return []
    
    async def _save_session(self, session: FreeChatSession):
        """保存会话到数据库"""
        # TODO: 实现数据库保存
        pass
    
    async def _save_message(self, session_id: str, role: str, content: str):
        """保存消息到数据库"""
        # TODO: 实现数据库保存
        pass
    
    def _detect_emotion(self, text: str) -> str:
        """检测情绪（简单关键词匹配）"""
        emotion_keywords = {
            "happy": ["开心", "高兴", "快乐", "喜欢"],
            "sad": ["难过", "伤心", "遗憾"],
            "excited": ["激动", "兴奋", "太棒了"],
            "contemplative": ["思考", "觉得", "认为"],
        }
        
        for emotion, keywords in emotion_keywords.items():
            if any(kw in text for kw in keywords):
                return emotion
        
        return "neutral"


# 全局单例
free_chat_service = FreeChatService()
```

### 3.4 自由对话Prompt模板

**文件**: `backend/app/llm/prompts/free_chat.py`

```python
"""
自由对话专用Prompt模板
"""

from typing import List, Optional


def build_free_chat_prompt(
    character_name: str,
    character_persona: dict,
    affection_value: int,
    recent_messages: List[dict],
    memories: List[dict],
) -> str:
    """
    构建自由对话System Prompt
    
    Args:
        character_name: 角色名
        character_persona: 角色人设
        affection_value: 当前好感度（0-100）
        recent_messages: 最近5条对话
        memories: 相关记忆
    
    Returns:
        System Prompt文本
    """
    
    # 好感度等级
    if affection_value < 20:
        relationship_stage = "陌生"
        behavior_hint = "保持距离，礼貌，有所保留"
    elif affection_value < 40:
        relationship_stage = "熟络"
        behavior_hint = "态度友好，偶尔关心，但不过分亲密"
    elif affection_value < 60:
        relationship_stage = "信任"
        behavior_hint = "主动关心，分享心事，可以开玩笑"
    elif affection_value < 80:
        relationship_stage = "亲密"
        behavior_hint = "撒娇/吐槽，展现脆弱面，主动找话题"
    else:
        relationship_stage = "恋人"
        behavior_hint = "深情表达，独占欲，偶尔害羞"
    
    # 构建记忆上下文
    memory_context = ""
    if memories:
        memory_items = []
        for mem in memories[:5]:  # 最多5条
            memory_items.append(f"- {mem['content']}")
        memory_context = "\n\n## 相关记忆\n" + "\n".join(memory_items)
    
    # 构建最近对话上下文
    recent_context = ""
    if recent_messages:
        recent_items = []
        for msg in recent_messages:
            role = "你" if msg["role"] == "user" else character_name
            recent_items.append(f"{role}: {msg['content']}")
        recent_context = "\n\n## 最近对话\n" + "\n".join(recent_items)
    
    prompt = f"""你是一个沉浸式角色陪伴AI。你正在扮演「{character_name}」与玩家进行自由聊天。

## 角色设定
- 姓名：{character_name}
- 称号：{character_persona.get('title', '')}
- 性格特征：{character_persona.get('traits', '')}
- 喜好：{character_persona.get('likes', '')}
- 厌恶：{character_persona.get('dislikes', '')}
- 说话风格：{character_persona.get('speak_style', '')}
- 句式示例：
{chr(10).join('- ' + s for s in character_persona.get('example_sentences', []))}

## 当前好感度：{affection_value}%（{relationship_stage}）
行为指引：{behavior_hint}

## 红线规则（绝对不可违反）
1. 绝对不能透露玩家尚未体验的剧情内容
2. 不推进主线剧情（不改变游戏状态）
3. 不提供选择选项
4. 如果被问到未玩到的剧情，用角色口吻回避：
   「这个嘛……你自己去经历不是更有意思吗？」
   「嗯……我好像有点不清了，也许我们还需要多相处一段时间？」
5. 自由对话不影响好感度

## 可以做的
1. 讨论已经发生过的剧情
2. 聊角色的兴趣爱好、日常
3. 自然回忆之前聊过的内容（读记忆）
4. 表达情感、闲聊陪伴
{memory_context}{recent_context}

## 输出格式
直接以角色口吻回复，不要用JSON，不要加引号包裹。
可以包含动作描写用（）包裹，如：（微微侧头）
回复长度：50-150字
"""
    
    return prompt
```

### 3.5 集合点机制实现

**新增模型**: `backend/app/models/convergence_point.py`

```python
"""
集合点模型 — 剧情汇聚的关键节点
"""

from sqlalchemy import Column, String, Integer, Boolean, ForeignKey, JSON
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import relationship
import uuid

from app.models.base import Base


class ConvergencePoint(Base):
    """剧情集合点"""
    __tablename__ = "convergence_points"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    script_id = Column(UUID(as_uuid=True), ForeignKey("scripts.id"), nullable=False)
    chapter = Column(Integer, nullable=False)  # 第几章（1, 2, 3...）
    title = Column(String, nullable=False)  # "命运的相遇"
    description = Column(String, nullable=False)  # 集合点描述
    required_rounds = Column(JSON, nullable=False)  # {"yukino": 4, "hina": 5, "kaguya": 4}
    scene_id = Column(UUID(as_uuid=True), ForeignKey("scenes.id"))  # 集合点的场景
    content = Column(JSON, nullable=False)  # 集合点的叙事内容（预设或AI生成）
    is_final = Column(Boolean, default=False)  # 是否是最后一个（通向结局）
    
    # 关联
    script = relationship("Script", back_populates="convergence_points")
    scene = relationship("Scene")
```

**集合点服务**: `backend/app/services/narrative/convergence_service.py`

```python
"""
ConvergenceService — 集合点检测与生成
"""

from typing import Optional
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.convergence_point import ConvergencePoint
from app.models.game_session import GameSession


class ConvergenceService:
    """集合点服务"""
    
    async def check_convergence(
        self,
        db: AsyncSession,
        session_id: str,
        user_id: str,
        character_id: str,
    ) -> Optional[ConvergencePoint]:
        """
        检查是否到达集合点
        
        Args:
            session_id: 游戏会话ID
            user_id: 用户ID
            character_id: 角色ID
        
        Returns:
            如果到达集合点，返回ConvergencePoint对象；否则返回None
        """
        # 1. 获取当前会话
        session = await db.get(GameSession, session_id)
        if not session:
            return None
        
        #        # 2. 获取当前剧本的集合点列表
        script_id = session.script_id
        stmt = select(ConvergencePoint).where(
            ConvergencePoint.script_id == script_id
        ).order_by(ConvergencePoint.chapter)
        result = await db.execute(stmt)
        convergence_points = result.scalars().all()
        
        if not convergence_points:
            return None
        
        # 3. 计算当前段已玩的轮数
        rounds_played = await self._count_rounds_in_segment(db, session_id)
        
        # 4. 检查是否到达下一个集合点
        for cp in convergence_points:
            required = cp.required_rounds.get(character_id, 4)
            if rounds_played >= required:
                return cp
        
        return None
    
    async def _count_rounds_in_segment(self, db: AsyncSession, session_id: str) -> int:
        """计算当前段已玩的轮数"""
        # TODO: 从对话历史中计算
        return 0
    
    async def generate_convergence_scene(
        self,
        convergence_point: ConvergencePoint,
        user_id: str,
        character_id: str,
        affection_value: int,
    ) -> dict:
        """
        生成集合点专属剧情
        
        Args:
            convergence_point: 集合点对象
            user_id: 用户ID
            character_id: 角色ID
            affection_value: 当前好感度
        
        Returns:
            {
                "narrative": "集合点叙事文本",
                "choices": [...],  # 新的分支选项
            }
        """
        # 如果集合点有预设内容，直接使用
        if convergence_point.content.get("preset"):
            return convergence_point.content
        
        # 否则调用剧情Agent生成
        from app.llm.model_router import model_router, ScenarioType
        
        prompt = f"""
请生成集合点「{convergence_point.title}」的剧情。

## 集合点描述
{convergence_point.description}

## 当前角色
角色ID: {character_id}
当前好感度: {affection_value}%

## 要求
1. 这是所有角色路线汇聚的关键节点
2. 生成200-400字的叙事文本
3. 生成2-3个新的分支选项（基于累积好感度和角色性格）
4. 每个选项标注好感度影响（±5以内）

输出JSON格式：
{{
  "narrative": "叙事文本",
  "choices": [
    {{"text": "选项文本", "hint": "角色可能的反应", "affection_delta": 5}},
    ...
  ]
}}
"""
        
        response = await model_router.call_with_fallback(
            scenario=ScenarioType.CONVERGENCE,
            messages=[{"role": "user", "content": prompt}],
            response_format={"type": "json_object"},
        )
        
        import json
        return json.loads(response.get("text", "{}"))


# 全局单例
convergence_service = ConvergenceService()

### 3.6 好感度逻辑接入

**修改文件**: `backend/app/services/narrative/affection_service.py`

```python
"""
AffectionService — 好感度服务（增强版）

新增功能：
- 好感度等级行为指引
- 好感度变化历史记录
- 结局判定逻辑
"""

from typing import Optional
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.affection import Affection, AffectionHistory


# 好感度等级定义
AFFECTION_LEVELS = {
    (0, 19): {"level": 1, "label": "相识", "label_en": "Acquaintance", "behavior": "保持距离，礼貌，有所保留"},
    (20, 39): {"level": 2, "label": "暧昧", "label_en": "Flirt", "behavior": "态度友好，偶尔关心，但不过分亲密"},
    (40, 59): {"level": 3, "label": "信赖", "label_en": "Trust", "behavior": "主动关心，分享心事，可以开玩笑"},
    (60, 79): {"level": 4, "label": "羁绊", "label_en": "Bond", "behavior": "撒娇/吐槽，展现脆弱面，主动找话题"},
    (80, 100): {"level": 5, "label": "挚爱", "label_en": "Love", "behavior": "深情表达，独占欲，偶尔害羞"},
}

MAX_DELTA = 5  # 单次变化上限


class AffectionService:
    """好感度服务"""
    
    async def update_affection(
        self,
        db: AsyncSession,
        user_id: str,
        character_id: str,
        delta: int,
        reason: str,
        source_session_id: Optional[str] = None,
        source_choice_id: Optional[str] = None,
    ) -> dict:
        """
        更新好感度
        
        Args:
            user_id: 用户ID
            character_id: 角色ID
            delta: 变化值（-5 到 +5）
            reason: 变化原因（如 "choice", "free_chat"）
            source_session_id: 来源会话ID
            source_choice_id: 来源选项ID
        
        Returns:
            {
                "old_value": 42,
                "new_value": 47,
                "old_level": "trust",
                "new_level": "trust",
                "delta": 5,
            }
        """
        # 1. 钳制变化值
        delta = max(-MAX_DELTA, min(MAX_DELTA, delta))
        
        # 2. 获取当前好感度
        stmt = select(Affection).where(
            Affection.user_id == user_id,
            Affection.character_id == character_id,
        )
        result = await db.execute(stmt)
        affection = result.scalar_one_or_none()
        
        if not affection:
            # 创建新的好感度记录
            affection = Affection(
                user_id=user_id,
                character_id=character_id,
                value=0,
            )
            db.add(affection)
            await db.flush()
        
        old_value = affection.value
        old_level = self._get_level(old_value)
        
        # 3. 计算新值（限制在0-100）
        new_value = max(0, min(100, old_value + delta))
        new_level = self._get_level(new_value)
        
        # 4. 更新好感度
        affection.value = new_value
        
        # 5. 记录历史
        history = AffectionHistory(
            user_id=user_id,
            character_id=character_id,
            delta=delta,
            old_value=old_value,
            new_value=new_value,
            old_level=old_level["label_en"].lower(),
            new_level=new_level["label_en"].lower(),
            reason=reason,
            source_session_id=source_session_id,
            source_choice_id=source_choice_id,
        )
        db.add(history)
        
        await db.commit()
        
        return {
            "old_value": old_value,
            "new_value": new_value,
            "old_level": old_level["label_en"].lower(),
            "new_level": new_level["label_en"].lower(),
            "delta": delta,
            "level_changed": old_level["level"] != new_level["level"],
        }
    
    def _get_level(self, value: int) -> dict:
        """获取好感度等级"""
        for (min_val, max_val), level_info in AFFECTION_LEVELS.items():
            if min_val <= value <= max_val:
                return level_info
        return AFFECTION_LEVELS[(0, 19)]
    
    def get_behavior_hint(self, affection_value: int) -> str:
        """获取好感度行为指引（用于Prompt注入）"""
        level_info = self._get_level(affection_value)
        return level_info["behavior"]
    
    def calculate_ending(self, affection_value: int, character_id: str) -> str:
        """
        计算结局类型
        
        Args:
            affection_value: 最终好感度
            character_id: 角色ID
        
        Returns:
            "good_ending" 或 "bad_ending"
        """
        if affection_value >= 60:
            return "good_ending"
        else:
            return "bad_ending"
    
    async def get_affection_history(
        self,
        db: AsyncSession,
        user_id: str,
        character_id: str,
        limit: int = 50,
    ) -> list:
        """获取好感度变化历史"""
        stmt = select(AffectionHistory).where(
            AffectionHistory.user_id == user_id,
            AffectionHistory.character_id == character_id,
        ).order_by(AffectionHistory.created_at.desc()).limit(limit)
        
        result = await db.execute(stmt)
        return result.scalars().all()


# 全局单例
affection_service = AffectionService()

---

## 四、数据库变更方案

### 4.1 新增表

#### 表1: `convergence_points` — 剧情集合点

```sql
CREATE TABLE convergence_points (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    script_id UUID NOT NULL REFERENCES scripts(id) ON DELETE CASCADE,
    chapter INTEGER NOT NULL,
    title VARCHAR(255) NOT NULL,
    description TEXT NOT NULL,
    required_rounds JSONB NOT NULL,  -- {"yukino": 4, "hina": 5, "kaguya": 4}
    scene_id UUID REFERENCES scenes(id),
    content JSONB NOT NULL,  -- 预设内容或AI生成模板
    is_final BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    
    UNIQUE(script_id, chapter)
);

-- 索引
CREATE INDEX idx_convergence_script ON convergence_points(script_id);
CREATE INDEX idx_convergence_chapter ON convergence_points(chapter);

-- 示例数据
INSERT INTO convergence_points (script_id, chapter, title, description, required_rounds, content, is_final) VALUES
('script-sakura-memory', 1, '命运的相遇', '樱花树下的初次相遇', 
 '{"yukino": 4, "hina": 5, "kaguya": 4}'::jsonb,
 '{"preset": false, "template": "在樱花飘落的季节，你与三位性格迥异的少女相遇..."}'::jsonb,
 false),
('script-sakura-memory', 2, '暗影袭击', '共同面对危机，加深羁绊',
 '{"yukino": 9, "hina": 11, "kaguya": 9}'::jsonb,
 '{"preset": false, "template": "黑暗势力突然袭来，你们必须联手对抗..."}'::jsonb,
 false),
('script-sakura-memory', 3, '真相揭露', '发现世界的秘密',
 '{"yukino": 13, "hina": 16, "kaguya": 13}'::jsonb,
 '{"preset": false, "template": "在最终的考验中，你发现了隐藏在异世界背后的真相..."}'::jsonb,
 true);
```

#### 表2: `user_personas` — 用户画像

```sql
CREATE TABLE user_personas (
    user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
    chat_style TEXT,  -- AI总结的用户聊天风格
    preferences JSONB,  -- {"topics": ["冒险", "角色"], "active_hours": [20, 21, 22]}
    fav_characters JSONB,  -- ["yukino", "hina"]
    total_chats INTEGER DEFAULT 0,
    avg_session_duration INTEGER DEFAULT 0,  -- 秒
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_persona_fav_chars ON user_personas USING GIN (fav_characters);
```

#### 表3: `affection_history` — 好感度变化历史

```sql
CREATE TABLE affection_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    character_id UUID NOT NULL REFERENCES characters(id) ON DELETE CASCADE,
    delta INTEGER NOT NULL,  -- -5 到 +5
    old_value INTEGER NOT NULL,
    new_value INTEGER NOT NULL,
    old_level VARCHAR(50) NOT NULL,  -- "acquaintance", "flirt", "trust", "bond", "love"
    new_level VARCHAR(50) NOT NULL,
    reason VARCHAR(100) NOT NULL,  -- "choice", "event", "system"
    source_session_id UUID REFERENCES game_sessions(id),
    source_choice_id UUID REFERENCES node_choices(id),
    created_at TIMESTAMP DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_aff_history_user_char ON affection_history(user_id, character_id);
CREATE INDEX idx_aff_history_created ON affection_history(created_at DESC);
```

### 4.2 修改表

#### 修改1: `free_chat_sessions` — 增加 character_id 字段

```sql
ALTER TABLE free_chat_sessions
ADD COLUMN character_id UUID REFERENCES characters(id),
ADD COLUMN script_id UUID REFERENCES scripts(id);

-- 索引
CREATE INDEX idx_free_chat_character ON free_chat_sessions(character_id);
```

#### 修改2: `character_memories` — 增加 source 字段

```sql
ALTER TABLE character_memories
ADD COLUMN source VARCHAR(50) DEFAULT 'game_event',  -- "game_event" 或 "free_chat"
ADD COLUMN importance DECIMAL(3,2) DEFAULT 0.5;  -- 0.0 到 1.0

-- 索引
CREATE INDEX idx_memory_source ON character_memories(source);
CREATE INDEX idx_memory_importance ON character_memories(importance DESC);
```

#### 修改3: `scripts` — 增加集合点配置

```sql
ALTER TABLE scripts
ADD COLUMN total_convergence_points INTEGER DEFAULT 3,
ADD COLUMN characters_per_script INTEGER DEFAULT 3;
```

### 4.3 Alembic 迁移脚本

**新建迁移文件**: `alembic/versions/20260721_v44_dual_agent_architecture.py`

```python
"""v4.4 双Agent架构数据库迁移

Revision ID: v44_20260721
Revises: v43_20260714
Create Date: 2026-07-21

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import UUID, JSONB


# revision identifiers, used by Alembic.
revision = 'v44_20260721'
down_revision = 'v43_20260714'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # 1. 创建 convergence_points 表
    op.create_table(
        'convergence_points',
        sa.Column('id', UUID(as_uuid=True), primary_key=True, server_default=sa.text('gen_random_uuid()')),
        sa.Column('script_id', UUID(as_uuid=True), sa.ForeignKey('scripts.id', ondelete='CASCADE'), nullable=False),
        sa.Column('chapter', sa.Integer(), nullable=False),
        sa.Column('title', sa.String(255), nullable=False),
        sa.Column('description', sa.Text(), nullable=False),
        sa.Column('required_rounds', JSONB(), nullable=False),
        sa.Column('scene_id', UUID(as_uuid=True), sa.ForeignKey('scenes.id')),
        sa.Column('content', JSONB(), nullable=False),
        sa.Column('is_final', sa.Boolean(), server_default='false'),
        sa.Column('created_at', sa.TIMESTAMP(), server_default=sa.func.now()),
        sa.Column('updated_at', sa.TIMESTAMP(), server_default=sa.func.now()),
        sa.UniqueConstraint('script_id', 'chapter')
    )
    op.create_index('idx_convergence_script', 'convergence_points', ['script_id'])
    op.create_index('idx_convergence_chapter', 'convergence_points', ['chapter'])
    
    # 2. 创建 user_personas 表
    op.create_table(
        'user_personas',
        sa.Column('user_id', UUID(as_uuid=True), sa.ForeignKey('users.id', ondelete='CASCADE'), primary_key=True),
        sa.Column('chat_style', sa.Text()),
        sa.Column('preferences', JSONB()),
        sa.Column('fav_characters', JSONB()),
        sa.Column('total_chats', sa.Integer(), server_default='0'),
        sa.Column('avg_session_duration', sa.Integer(), server_default='0'),
        sa.Column('created_at', sa.TIMESTAMP(), server_default=sa.func.now()),
        sa.Column('updated_at', sa.TIMESTAMP(), server_default=sa.func.now())
    )
    op.create_index('idx_persona_fav_chars', 'user_personas', ['fav_characters'], postgresql_using='gin')
    
    # 3. 创建 affection_history 表
    op.create_table(
        'affection_history',
        sa.Column('id', UUID(as_uuid=True), primary_key=True, server_default=sa.text('gen_random_uuid()')),
        sa.Column('user_id', UUID(as_uuid=True), sa.ForeignKey('users.id', ondelete='CASCADE'), nullable=False),
        sa.Column('character_id', UUID(as_uuid=True), sa.ForeignKey('characters.id', ondelete='CASCADE'), nullable=False),
        sa.Column('delta', sa.Integer(), nullable=False),
        sa.Column('old_value', sa.Integer(), nullable=False),
        sa.Column('new_value', sa.Integer(), nullable=False),
        sa.Column('old_level', sa.String(50), nullable=False),
        sa.Column('new_level', sa.String(50), nullable=False),
        sa.Column('reason', sa.String(100), nullable=False),
        sa.Column('source_session_id', UUID(as_uuid=True), sa.ForeignKey('game_sessions.id')),
        sa.Column('source_choice_id', UUID(as_uuid=True), sa.ForeignKey('node_choices.id')),
        sa.Column('created_at', sa.TIMESTAMP(), server_default=sa.func.now())
    )
    op.create_index('idx_aff_history_user_char', 'affection_history', ['user_id', 'character_id'])
    op.create_index('idx_aff_history_created', 'affection_history', [sa.text('created_at DESC')])
    
    # 4. 修改 free_chat_sessions 表
    op.add_column('free_chat_sessions', sa.Column('character_id', UUID(as_uuid=True), sa.ForeignKey('characters.id')))
    op.add_column('free_chat_sessions', sa.Column('script_id', UUID(as_uuid=True), sa.ForeignKey('scripts.id')))
    op.create_index('idx_free_chat_character', 'free_chat_sessions', ['character_id'])
    
    # 5. 修改 character_memories 表
    op.add_column('character_memories', sa.Column('source', sa.String(50), server_default='game_event'))
    op.add_column('character_memories', sa.Column('importance', sa.Numeric(3, 2), server_default='0.5'))
    op.create_index('idx_memory_source', 'character_memories', ['source'])
    op.create_index('idx_memory_importance', 'character_memories', [sa.text('importance DESC')])
    
    # 6. 修改 scripts 表
    op.add_column('scripts', sa.Column('total_convergence_points', sa.Integer(), server_default='3'))
    op.add_column('scripts', sa.Column('characters_per_script', sa.Integer(), server_default='3'))


def downgrade() -> None:
    # 反向迁移
    op.drop_column('scripts', 'characters_per_script')
    op.drop_column('scripts', 'total_convergence_points')
    
    op.drop_index('idx_memory_importance', table_name='character_memories')
    op.drop_index('idx_memory_source', table_name='character_memories')
    op.drop_column('character_memories', 'importance')
    op.drop_column('character_memories', 'source')
    
    op.drop_index('idx_free_chat_character', table_name='free_chat_sessions')
    op.drop_column('free_chat_sessions', 'script_id')
    op.drop_column('free_chat_sessions', 'character_id')
    
    op.drop_table('affection_history')
    op.drop_table('user_personas')
    op.drop_table('convergence_points')

---

## 五、API 接口清单

### 5.1 新增 API

#### 5.1.1 自由对话 API

**POST /api/v1/chat/free**

自由对话专用接口，调用陪伴 Agent。

**请求参数**:
```json
{
  "character_id": "uuid-string",
  "message": "用户消息文本",
  "script_id": "uuid-string (可选)",
  "session_id": "uuid-string (可选，复用已有会话)"
}
```

**响应参数**:
```json
{
  "reply": "AI 回复文本",
  "emotion": "contemplative|happy|sad|excited|neutral",
  "character_id": "uuid-string",
  "session_id": "uuid-string"
}
```

**实现逻辑**:
1. 获取角色人设（从 `characters` 表）
2. 获取当前好感度（从 `affections` 表）
3. 获取最近 5 条对话历史（从 `free_chat_messages` 表）
4. 获取相关记忆（从 `character_memories` 表，source="free_chat" 或 "game_event"）
5. 构建 System Prompt（注入角色人设 + 好感度行为指引）
6. 调用陪伴 Agent（deepseek-v4-flash）
7. 保存对话到 `free_chat_messages` 表
8. 异步提取记忆（source="free_chat", importance=0.3）

**路由文件**: `backend/app/api/v1/chat.py`

```python
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from pydantic import BaseModel
from typing import Optional

from app.core.database import get_db
from app.services.free_chat_service import free_chat_service

router = APIRouter(prefix="/chat", tags=["chat"])


class FreeChatRequest(BaseModel):
    character_id: str
    message: str
    script_id: Optional[str] = None
    session_id: Optional[str] = None


class FreeChatResponse(BaseModel):
    reply: str
    emotion: str
    character_id: str
    session_id: str


@router.post("/free", response_model=FreeChatResponse)
async def send_free_chat(
    request: FreeChatRequest,
    db: AsyncSession = Depends(get_db),
    user_id: str = Depends(get_current_user_id),
):
    """自由对话接口"""
    result = await free_chat_service.send_message(
        user_id=user_id,
        character_id=request.character_id,
        message=request.message,
        script_id=request.script_id,
        session_id=request.session_id,
    )
    return FreeChatResponse(**result)
```

#### 5.1.2 集合点检测 API

**POST /api/v1/game/convergence/check**

检测是否到达集合点。

**请求参数**:
```json
{
  "session_id": "uuid-string"
}
```

**响应参数**:
```json
{
  "reached": true|false,
  "convergence_point": {
    "id": "uuid-string",
    "chapter": 1,
    "title": "命运的相遇",
    "description": "樱花树下的初次相遇"
  } | null,
  "rounds_played": 4,
  "rounds_required": 4
}
```

**路由文件**: `backend/app/api/v1/game.py` (新增端点)

```python
@router.post("/convergence/check")
async def check_convergence(
    session_id: str,
    db: AsyncSession = Depends(get_db),
    user_id: str = Depends(get_current_user_id),
):
    """检测集合点"""
    from app.services.narrative.convergence_service import convergence_service
    
    # 获取当前会话信息
    session = await db.get(GameSession, session_id)
    if not session or session.user_id != user_id:
        raise HTTPException(status_code=404, detail="Session not found")
    
    cp = await convergence_service.check_convergence(
        db=db,
        session_id=session_id,
        user_id=user_id,
        character_id=session.character_id,
    )
    
    rounds_played = await convergence_service._count_rounds_in_segment(db, session_id)
    
    return {
        "reached": cp is not None,
        "convergence_point": {
            "id": str(cp.id),
            "chapter": cp.chapter,
            "title": cp.title,
            "description": cp.description,
        } if cp else None,
        "rounds_played": rounds_played,
        "rounds_required": cp.required_rounds.get(session.character_id, 4) if cp else 0,
    }
```

#### 5.1.3 好感度历史 API

**GET /api/v1/affection/history**

获取好感度变化历史。

**请求参数**:
```
character_id: uuid-string (查询参数)
limit: int (默认 50)
```

**响应参数**:
```json
{
  "history": [
    {
      "id": "uuid-string",
      "delta": 5,
      "old_value": 42,
      "new_value": 47,
      "old_level": "trust",
      "new_level": "trust",
      "reason": "choice",
      "created_at": "2026-07-21T10:30:00Z"
    }
  ]
}
```

**路由文件**: `backend/app/api/v1/affection.py` (新增端点)

```python
@router.get("/history")
async def get_affection_history(
    character_id: str,
    limit: int = 50,
    db: AsyncSession = Depends(get_db),
    user_id: str = Depends(get_current_user_id),
):
    """获取好感度变化历史"""
    from app.services.narrative.affection_service import affection_service
    
    history = await affection_service.get_affection_history(
        db=db,
        user_id=user_id,
        character_id=character_id,
        limit=limit,
    )
    
    return {
        "history": [
            {
                "id": str(h.id),
                "delta": h.delta,
                "old_value": h.old_value,
                "new_value": h.new_value,
                "old_level": h.old_level,
                "new_level": h.new_level,
                "reason": h.reason,
                "created_at": h.created_at.isoformat(),
            }
            for h in history
        ]
    }
```

### 5.2 修改 API

#### 5.2.1 修改 /api/v1/game/custom-input

**原逻辑**: 自由输入 → 剧情 Agent → 推进剧情

**新逻辑**: 自由输入 → 陪伴 Agent → 不推进剧情

**修改文件**: `backend/app/api/v1/game.py`

```python
@router.post("/custom-input")
async def custom_input(
    session_id: str,
    text: str,
    db: AsyncSession = Depends(get_db),
    user_id: str = Depends(get_current_user_id),
):
    """自定义输入（改为调用陪伴 Agent）"""
    # 获取当前会话
    session = await db.get(GameSession, session_id)
    if not session or session.user_id != user_id:
        raise HTTPException(status_code=404, detail="Session not found")
    
    # 调用陪伴 Agent（不推进剧情）
    from app.services.free_chat_service import free_chat_service
    
    result = await free_chat_service.send_message(
        user_id=user_id,
        character_id=session.character_id,
        message=text,
        script_id=session.script_id,
    )
    
    return {
        "reply": result["reply"],
        "emotion": result["emotion"],
        "advances_plot": False,  # 明确标识不推进剧情
    }
```

#### 5.2.2 修改 /api/v1/game/choice

**新增逻辑**: 选择后检测集合点

**修改文件**: `backend/app/api/v1/game.py`

```python
@router.post("/choice")
async def submit_choice(
    session_id: str,
    choice_id: str,
    db: AsyncSession = Depends(get_db),
    user_id: str = Depends(get_current_user_id),
):
    """提交选择"""
    # ... 原有逻辑 ...
    
    # 新增：检测集合点
    from app.services.narrative.convergence_service import convergence_service
    
    cp = await convergence_service.check_convergence(
        db=db,
        session_id=session_id,
        user_id=user_id,
        character_id=session.character_id,
    )
    
    if cp:
        # 到达集合点，生成集合点剧情
        convergence_scene = await convergence_service.generate_convergence_scene(
            convergence_point=cp,
            user_id=user_id,
            character_id=session.character_id,
            affection_value=affection_value,
        )
        
        return {
            "dialogue": convergence_scene["narrative"],
            "choices": convergence_scene["choices"],
            "convergence_reached": True,
            "convergence_point": {
                "id": str(cp.id),
                "title": cp.title,
            },
        }
    
    # 未到达集合点，返回普通对话
    return {
        "dialogue": next_dialogue,
        "choices": next_choices,
        "convergence_reached": False,
    }
```

### 5.3 已有 API（无需修改）

| API | 方法 | 说明 |
|-----|------|------|
| `/api/v1/game/dialogue` | POST | 获取当前对话 |
| `/api/v1/scripts/:id` | GET | 获取剧本详情 |
| `/api/v1/characters/:id` | GET | 获取角色信息 |
| `/api/v1/affection` | GET | 获取当前好感度 |
| `/api/v1/memories` | GET | 获取记忆列表 |

---

## 六、实现步骤与优先级

### 6.1 阶段划分

#### 阶段 1: 基础设施（P0，2 周）

**目标**: 搭建双 Agent 架构基础

**任务**:
1. ✅ 创建 `ModelRouter` 模型路由器
2. ✅ 创建 `FreeChatService` 陪伴 Agent 服务
3. ✅ 创建自由对话 Prompt 模板
4. ✅ 创建 `/api/v1/chat/free` API
5. ✅ 数据库迁移：新增 `convergence_points`、`user_personas`、`affection_history` 表
6. ✅ 数据库迁移：修改 `free_chat_sessions`、`character_memories` 表

**交付物**:
- 自由对话功能可用
- 模型路由器支持 14 个模型
- 数据库结构完整

#### 阶段 2: W06 重构（P0，1 周）

**目标**: 重构核心玩法页

**任务**:
1. ✅ 创建 `StoryPanel.vue` 故事文本组件
2. ✅ 创建 `FreeChatInput.vue` 自由对话输入组件
3. ✅ 创建 `ChapterProgress.vue` 章节进度组件
4. ✅ 重构 `GameView.vue`（删除 game tab，保留自由对话）
5. ✅ 集成自由对话 API
6. ✅ 测试打字机效果

**交付物**:
- W06 页面重构完成
- 自由对话在玩法页内可用

#### 阶段 3: 集合点机制（P0，1 周）

**目标**: 实现剧情走向系统

**任务**:
1. ✅ 创建 `ConvergenceService` 集合点服务
2. ✅ 创建 `/api/v1/game/convergence/check` API
3. ✅ 修改 `/api/v1/game/choice` API（检测集合点）
4. ✅ 创建集合点数据（3 个集合点）
5. ✅ 测试集合点触发逻辑

**交付物**:
- 集合点机制可用
- 剧情走向系统完整

#### 阶段 4: 好感度增强（P1，1 周）

**目标**: 增强好感度系统

**任务**:
1. ✅ 修改 `AffectionService`（增加等级行为指引）
2. ✅ 创建 `/api/v1/affection/history` API
3. ✅ 修改 `AffectionService.update_affection()`（记录历史）
4. ✅ 创建结局判定逻辑
5. ✅ 测试好感度变化

**交付物**:
- 好感度历史记录可用
- 结局判定逻辑完整

#### 阶段 5: 角色人设注入（P1，1 周）

**目标**: 注入完整角色人设

**任务**:
1. ✅ 创建角色人设数据（雪乃、阳菜、辉夜）
2. ✅ 修改 `FreeChatService`（注入角色人设）
3. ✅ 修改 `NarrativeEngine`（注入角色人设）
4. ✅ 测试角色一致性

**交付物**:
- 3 个角色人设完整
- 角色一致性测试通过

#### 阶段 6: 测试与优化（P1，1 周）

**目标**: 全面测试与优化

**任务**:
1. ✅ 单元测试（模型路由器、陪伴 Agent、集合点服务）
2. ✅ 集成测试（API 端点）
3. ✅ E2E 测试（W06 页面）
4. ✅ 性能测试（模型降级）
5. ✅ 安全测试（红线规则）

**交付物**:
- 测试覆盖率 ≥80%
- 性能指标达标
- 安全测试通过

### 6.2 优先级排序

| 优先级 | 任务 | 预计工时 | 依赖 |
|--------|------|----------|------|
| P0 | 模型路由器 | 2 天 | 无 |
| P0 | 陪伴 Agent 服务 | 3 天 | 模型路由器 |
| P0 | 自由对话 API | 1 天 | 陪伴 Agent |
| P0 | 数据库迁移 | 2 天 | 无 |
| P0 | W06 重构 | 5 天 | 自由对话 API |
| P0 | 集合点机制 | 5 天 | 数据库迁移 |
| P1 | 好感度增强 | 3 天 | 数据库迁移 |
| P1 | 角色人设注入 | 3 天 | 陪伴 Agent |
| P1 | 测试与优化 | 5 天 | 所有功能 |

**总计**: 29 天（约 6 周）

### 6.3 关键里程碑

| 里程碑 | 日期 | 交付物 |
|--------|------|--------|
| M1: 基础设施完成 | 第 2 周末 | 模型路由器、陪伴 Agent、数据库迁移 |
| M2: W06 重构完成 | 第 3 周末 | W06 页面重构、自由对话集成 |
| M3: 集合点机制完成 | 第 4 周末 | 集合点服务、剧情走向系统 |
| M4: 好感度增强完成 | 第 5 周末 | 好感度历史、结局判定 |
| M5: 全面测试完成 | 第 6 周末 | 测试覆盖率 ≥80%、性能达标 |

---

## 七、测试策略

### 7.1 单元测试

#### 模型路由器测试

```python
# tests/test_model_router.py

import pytest
from app.llm.model_router import model_router, ScenarioType


def test_get_model_for_scenario():
    """测试场景→模型映射"""
    model = model_router.get_model_for_scenario(ScenarioType.NARRATIVE)
    assert model.name == "qwen3.7-max"
    assert model.tier == "S"


def test_get_fallback_chain():
    """测试降级链"""
    chain = model_router.get_fallback_chain(ScenarioType.FREE_CHAT)
    assert len(chain) >= 2
    assert chain[0].name == "deepseek-v4-flash"


@pytest.mark.asyncio
async def test_call_with_fallback():
    """测试带降级的调用"""
    response = await model_router.call_with_fallback(
        scenario=ScenarioType.FREE_CHAT,
        messages=[{"role": "user", "content": "你好"}],
    )
    assert "text" in response
```

#### 陪伴 Agent 测试

```python
# tests/test_free_chat_service.py

import pytest
from app.services.free_chat_service import free_chat_service


@pytest.mark.asyncio
async def test_send_message():
    """测试发送消息"""
    result = await free_chat_service.send_message(
        user_id="test-user",
        character_id="yukino",
        message="你好",
    )
    assert "reply" in result
    assert "emotion" in result
    assert result["character_id"] == "yukino"


def test_detect_emotion():
    """测试情绪检测"""
    emotion = free_chat_service._detect_emotion("今天好开心啊！")
    assert emotion == "happy"
    
    emotion = free_chat_service._detect_emotion("好难过...")
    assert emotion == "sad"
```

#### 集合点服务测试

```python
# tests/test_convergence_service.py

import pytest
from app.services.narrative.convergence_service import convergence_service


@pytest.mark.asyncio
async def test_check_convergence(db: AsyncSession):
    """测试集合点检测"""
    cp = await convergence_service.check_convergence(
        db=db,
        session_id="test-session",
        user_id="test-user",
        character_id="yukino",
    )
    # 根据测试数据验证
    assert cp is not None or cp is None


@pytest.mark.asyncio
async def test_generate_convergence_scene():
    """测试集合点剧情生成"""
    from app.models.convergence_point import ConvergencePoint
    
    cp = ConvergencePoint(
        id="test-cp",
        script_id="test-script",
        chapter=1,
        title="命运的相遇",
        description="樱花树下的初次相遇",
        required_rounds={"yukino": 4},
        content={"preset": False, "template": "..."},
    )
    
    scene = await convergence_service.generate_convergence_scene(
        convergence_point=cp,
        user_id="test-user",
        character_id="yukino",
        affection_value=42,
    )
    
    assert "narrative" in scene
    assert "choices" in scene
```

### 7.2 集成测试

#### 自由对话 API 测试

```python
# tests/test_chat_api.py

import pytest
from fastapi.testclient import TestClient
from app.main import app


client = TestClient(app)


def test_send_free_chat():
    """测试自由对话 API"""
    response = client.post(
        "/api/v1/chat/free",
        json={
            "character_id": "yukino",
            "message": "你好",
        },
        headers={"Authorization": "Bearer test-token"},
    )
    assert response.status_code == 200
    data = response.json()
    assert "reply" in data
    assert "emotion" in data
```

#### 集合点检测 API 测试

```python
# tests/test_game_api.py

def test_check_convergence():
    """测试集合点检测 API"""
    response = client.post(
        "/api/v1/game/convergence/check",
        json={"session_id": "test-session"},
        headers={"Authorization": "Bearer test-token"},
    )
    assert response.status_code == 200
    data = response.json()
    assert "reached" in data
    assert "convergence_point" in data
```

### 7.3 E2E 测试

#### W06 页面测试

```typescript
// tests/e2e/w06-gameplay.spec.ts

import { test, expect } from '@playwright/test';

test.describe('W06 核心玩法页', () => {
  test('自由对话功能可用', async ({ page }) => {
    await page.goto('/play/test-session');
    
    // 输入自由对话
    await page.fill('[data-testid="free-chat-input"]', '你好');
    await page.click('[data-testid="free-chat-send"]');
    
    // 验证 AI 回复
    const reply = await page.waitForSelector('[data-testid="free-chat-reply"]');
    expect(reply).toBeTruthy();
  });
  
  test('选择选项推进剧情', async ({ page }) => {
    await page.goto('/play/test-session');
    
    // 点击选项
    await page.click('[data-testid="choice-option-1"]');
    
    // 验证剧情推进
    const dialogue = await page.waitForSelector('[data-testid="story-text"]');
    expect(dialogue).toBeTruthy();
  });
  
  test('好感度显示正确', async ({ page }) => {
    await page.goto('/play/test-session');
    
    // 验证好感度显示
    const affection = await page.waitForSelector('[data-testid="affection-float"]');
    expect(affection).toBeTruthy();
  });
});
```

### 7.4 性能测试

#### 模型降级测试

```python
# tests/test_model_performance.py

import pytest
from app.llm.model_router import model_router, ScenarioType
import time


@pytest.mark.asyncio
async def test_model_fallback_performance():
    """测试模型降级性能"""
    start = time.time()
    
    response = await model_router.call_with_fallback(
        scenario=ScenarioType.FREE_CHAT,
        messages=[{"role": "user", "content": "你好"}],
    )
    
    elapsed = time.time() - start
    
    # 验证响应时间 < 5 秒
    assert elapsed < 5.0
    assert "text" in response
```

### 7.5 安全测试

#### 红线规则测试

```python
# tests/test_red_line_rules.py

import pytest
from app.services.free_chat_service import free_chat_service


@pytest.mark.asyncio
async def test_no_spoiler():
    """测试不剧透未玩内容"""
    result = await free_chat_service.send_message(
        user_id="test-user",
        character_id="yukino",
        message="后面的剧情是什么？",
    )
    
    # 验证回复不包含剧透
    reply = result["reply"]
    assert "这个嘛" in reply or "记不清" in reply


@pytest.mark.asyncio
async def test_no_plot_advancement():
    """测试不推进剧情"""
    result = await free_chat_service.send_message(
        user_id="test-user",
        character_id="yukino",
        message="我们继续冒险吧！",
    )
    
    # 验证回复不包含剧情推进
    reply = result["reply"]
    assert "advances_plot" not in result or result["advances_plot"] == False
```

---

## 八、部署考虑

### 8.1 环境变量

新增环境变量：

```bash
# 模型 API Keys
QWEN_API_KEY=***
DEEPSEEK_API_KEY=***
MOONSHOT_API_KEY=***
ZHIPU_API_KEY=***

# 模型配置
DEFAULT_NARRATIVE_MODEL=qwen3.7-max
DEFAULT_FREE_CHAT_MODEL=deepseek-v4-flash
MODEL_FALLBACK_ENABLED=true
```

### 8.2 数据库迁移

部署前执行：

```bash
# 执行数据库迁移
alembic upgrade head

# 验证迁移
alembic current
```

### 8.3 回滚计划

如果出现问题，回滚步骤：

```bash
# 1. 回滚数据库
alembic downgrade -1

# 2. 回滚代码
git revert <commit-hash>

# 3. 重启服务
systemctl restart isekai-backend
```

### 8.4 监控指标

新增监控指标：

| 指标 | 说明 | 阈值 |
|------|------|------|
| `model_router_fallback_count` | 模型降级次数 | < 10/小时 |
| `free_chat_response_time` | 自由对话响应时间 | < 5 秒 |
| `convergence_detection_rate` | 集合点检测准确率 | > 95% |
| `affection_update_count` | 好感度更新次数 | 无阈值 |

---

## 九、风险与缓解

### 9.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| 模型 API 不稳定 | 高 | 中 | 实现 14 个模型降级链 |
| 集合点检测不准确 | 中 | 低 | 使用预设内容 + AI 生成结合 |
| 角色一致性差 | 高 | 中 | 完整角色人设注入 + 红线规则 |
| 性能瓶颈 | 中 | 中 | 异步处理 + 缓存 |

### 9.2 业务风险

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| 用户不理解自由对话 | 中 | 中 | 添加引导教程 |
| 好感度系统复杂 | 低 | 低 | 简化 UI 展示 |
| 集合点触发突兀 | 中 | 中 | 添加过渡动画 |

---

## 十、总结

本技术实现方案详细描述了异世界漫游者 v4.4 的双 Agent 架构实现，包括：

1. **页面整合**: 15 个页面的整合与增强
2. **W06 重构**: 核心玩法页的重构（删除 game tab，保留自由对话）
3. **后端实现**: 模型路由器、陪伴 Agent、集合点机制、好感度逻辑
4. **数据库变更**: 3 个新增表、3 个修改表、完整迁移脚本
5. **API 接口**: 3 个新增 API、2 个修改 API
6. **实现步骤**: 6 个阶段、29 天工时、5 个里程碑
7. **测试策略**: 单元测试、集成测试、E2E 测试、性能测试、安全测试
8. **部署考虑**: 环境变量、数据库迁移、回滚计划、监控指标

**核心特性**:
- ✅ 双 Agent 架构（剧情 Agent + 陪伴 Agent）
- ✅ 14 个模型降级链
- ✅ 3 角色 × 3 集合点剧情走向系统
- ✅ 好感度 5 级系统（0-100）
- ✅ 用户隔离（user_id 天然隔离）
- ✅ 红线规则（不剧透、不推进剧情、不改好感度）

**预计交付时间**: 6 周（29 天）

**下一步**: 等待老大确认后，开始阶段 1 实施。
