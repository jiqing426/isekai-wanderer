# Docker

容器部署模板目录。用于保存 Dockerfile 示例、生产 Compose 示例或平台编排配置。

## 约定

- 镜像构建应可重复，不依赖开发者本机状态。
- 运行时配置通过环境变量、密钥管理或部署平台注入。
- 镜像中不得包含 `.env`、真实密钥、测试数据转储或构建缓存。
- 生产部署变化同步 `../../docs/operations/operations.md`。
