# OpenSpec Project Context

Project-level facts live in `../PROJECT.md`. This file exists so OpenSpec tooling has a standard project context entry.

Do not duplicate long-term project facts here. Link back to `../PROJECT.md` and the relevant `../docs/` file when a change needs context.
