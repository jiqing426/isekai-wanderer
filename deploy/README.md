# Deploy

部署模板目录，记录容器、反向代理、环境配置、发布检查和回滚要求。

## 目录约定

- `nginx/`：Nginx 或等价反向代理配置模板。
- `docker/`：容器镜像、生产 Compose 或编排配置模板。
- `production-checklist.md`：生产发布前后的强制检查清单。

部署配置不得写入真实密钥、证书私钥或生产专属敏感值。环境差异应通过环境变量、密钥管理系统或部署平台配置注入。
