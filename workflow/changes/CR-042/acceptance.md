# Acceptance

验收覆盖矩阵。每个 P0/P1 验收项必须能从 OpenSpec Requirement / Scenario 追到 OpenSpec task、实现证据、测试证据和覆盖结论。

| 验收编号 | 需求编号 | 优先级 | 来源规格 | 验收标准 | 设计落点 | OpenSpec Task | 实现证据 | 测试用例 / 验证命令 | 状态 | 覆盖状态 | 未覆盖原因 | PL 处理 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-001 | REQ-001 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 用户在 Legacy 引擎游戏会话中点击选项（submit_choice），推进到 transition 节点时，后端返回 text/event-stream，前端逐字显示对话文本。用户动作：点击选项；可观察结果：对话逐字显示。 | §2.1 `submit_choice` Legacy 分支条件 SSE：先执行 `process_choice()` 推进节点，检查 `next_node.node_type`；transition 节点 → `_stream_legacy_turn()` 返回 `StreamingResponse(text/event-stream)`；使用 `llm_gateway.stream_dialogue()` 逐 token yield `{"type":"text","content":"..."}` 事件 | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_submit_choice_sse.py -v (backend/tests/integration/test_legacy_submit_choice_sse.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 Browser Interaction E2E；需要 API/DB/Runtime 契约验证。SSE 事件格式见 AC-003 |
| AC-002 | REQ-001 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 用户在 Legacy 引擎游戏会话中点击选项（submit_choice），推进到 preset/choice 节点时，后端返回标准 JSON 响应，快速返回预设对话内容和选项，不触发 SSE 流。用户动作：点击选项；可观察结果：JSON 响应快速返回，无逐字显示。 | §2.1 `submit_choice` Legacy 分支条件判断：`next_node.node_type` 为 preset/choice → 保持 JSON 响应（`return result` 现有行为不变） | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_submit_choice_sse.py -v (backend/tests/integration/test_legacy_submit_choice_sse.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 Browser Interaction E2E。CEO 附条件 C3 |
| AC-003 | REQ-001 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | SSE 事件格式与 Corvus 路径一致：data: {"type":"text","content":"..."} 逐字推送，data: {"type":"done",...} 结束。验证 SSE 事件类型和 content 字段。 | §1 SSE 事件格式：Legacy 路径使用 `data: {"type":"text","content":"..."}\n\n` 逐字推送，`data: {"type":"done",...}\n\n` 结束，与 Corvus 路径格式一致 | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_sse_event_format.py -v (backend/tests/integration/test_legacy_sse_event_format.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 API/DB/Runtime 契约验证 |
| AC-004 | REQ-001 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | SSE 流结束后在 done 事件中推送好感度变化、成就解锁等元数据。数据状态：好感度变化异步写入数据库，刷新后仍然存在。验证：SSE 流结束后查询好感度确认更新。 | §4 DB 写入时序：SSE 流结束后 `finally` 块通过 `asyncio.create_task(_run_legacy_deferred())` 异步写入好感度；`done` 事件携带 `affection_change` 字段 | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_deferred_db_write.py -v (backend/tests/integration/test_legacy_deferred_db_write.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 API/DB/Runtime 契约验证。复用 Corvus _run_deferred 模式 |
| AC-005 | REQ-001 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 前端在 SSE 流中直接获得对话内容，不再发送 fetchDialogue() 二次请求。用户动作：用户点击选项后观察对话显示；可观察结果：Network 面板无 fetchDialogue 请求。 | §2.1 `done` 事件携带 `node_id` + `choices`，前端直接更新 `currentSession.current_node_id` 和 `pendingChoices`，不需要 `fetchDialogue()` 二次请求；§3.2 前端使用 `useSSEStream()` composable 的 `onDone` callback 处理 | DEV-002 | `frontend/src/stores/game.ts` submitChoice Legacy SSE branch uses consumeSSEBody with onDone callback that sets node_id + choices without fetchDialogue(); `frontend/src/composables/useSSEStream.ts` composable; `tests/unit/fe/useSSEStream.test.ts` 18/18 pass | npx playwright test frontend/tests/e2e/cr042-legacy-sse.spec.ts --project=chromium --trace on (frontend/tests/e2e/cr042-legacy-sse.spec.ts) | Verified | covered | DEV-002 实现完成，18/18 单元测试通过，TypeScript 编译 0 错误 | 待 Browser E2E QA 验证 | 需要 Browser Interaction E2E |
| AC-006 | REQ-002 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 用户在 Legacy 引擎游戏会话中填写自由文本并提交（submit_custom_input），后端返回 text/event-stream。用户动作：填写自由文本并提交；可观察结果：收到 text/event-stream 响应。 | §2.2 `submit_custom_input` Legacy 分支改造为返回 `StreamingResponse(text/event-stream)`，调用 `_stream_legacy_custom_input()` 函数 | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_submit_custom_input_sse.py -v (backend/tests/integration/test_legacy_submit_custom_input_sse.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 Browser Interaction E2E；需要 API/DB/Runtime 契约验证 |
| AC-007 | REQ-002 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 后端使用 llm_gateway.stream_dialogue() 替代同步 generate_dialogue() 进行流式输出。验证代码调用 stream_dialogue 而非 generate_dialogue。 | §2.2 `_stream_legacy_custom_input()` 中调用 `llm_gateway.stream_dialogue()` 替代 `_generate_custom_response()` 同步方法 | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_submit_custom_input_sse.py -v (backend/tests/integration/test_legacy_submit_custom_input_sse.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 API/DB/Runtime 契约验证 |
| AC-008 | REQ-002 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | SSE 流式逐字输出角色回应文本。用户动作：用户提交自由输入；可观察结果：角色回应文本逐字显示。 | §2.2 `llm_gateway.stream_dialogue()` 返回 `AsyncGenerator[str, None]`，`_stream_legacy_custom_input()` 逐 chunk 包装为 `data: {"type":"text","content":"..."}` 事件 yield | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_submit_custom_input_sse.py -v (backend/tests/integration/test_legacy_submit_custom_input_sse.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 Browser Interaction E2E |
| AC-009 | REQ-002 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | SSE 流结束后在 done 事件中推送好感度变化和下一节点选项。数据状态：好感度变化异步写入数据库，刷新后仍然存在。验证：SSE 流结束后查询好感度确认更新。 | §2.2 `done` 事件携带 `affection_change` 和 `choices`；deferred task 异步写入好感度和推进节点 | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_deferred_db_write.py -v (backend/tests/integration/test_legacy_deferred_db_write.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 API/DB/Runtime 契约验证 |
| AC-010 | REQ-002 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 前端 Legacy 分支使用 SSE reader 解析，角色回应逐字显示。用户动作：用户提交自由输入后观察角色回应显示；可观察结果：角色回应逐字显示。 | §3.2 前端 Legacy 分支 `submitCustomInput()` 使用 `useSSEStream()` composable 的 `onText` callback 累积 `fullText` 并实时更新 `currentDialogue.text` | DEV-002 | `frontend/src/stores/game.ts` submitCustomInput uses `useSSEStream()` composable (Corvus+Legacy unified); `tests/unit/fe/useSSEStream.test.ts` 18/18 pass | npx playwright test frontend/tests/e2e/cr042-legacy-sse.spec.ts --project=chromium --trace on (frontend/tests/e2e/cr042-legacy-sse.spec.ts) | Verified | covered | DEV-002 实现完成，18/18 单元测试通过 | 待 Browser E2E QA 验证 | 需要 Browser Interaction E2E。使用 useSSEStream composable（见 AC-020） |
| AC-011 | REQ-003 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 新增 POST /game/{session_id}/free-chat/stream 端点，返回 text/event-stream。响应头包含 X-Accel-Buffering: no。用户动作：前端调用流式端点；可观察结果：后端返回 text/event-stream。 | §2.3 新增 `POST /game/{session_id}/free-chat/stream` 端点，返回 `StreamingResponse(text/event-stream)` + headers 包含 `X-Accel-Buffering: no` | DEV-001 | 待填写 | pytest backend/tests/integration/test_free_chat_stream_endpoint.py -v (backend/tests/integration/test_free_chat_stream_endpoint.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 API/DB/Runtime 契约验证 |
| AC-012 | REQ-003 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | model_router 新增 stream_with_fallback() 方法支持流式 + 降级。验证 stream_with_fallback 方法存在且可调用。 | §2.4 `model_router.stream_with_fallback(scenario, messages, **kwargs) -> AsyncGenerator[str, None]` 方法签名，返回异步生成器 | DEV-001 | 待填写 | pytest backend/tests/unit/test_model_router_stream.py -v (backend/tests/unit/test_model_router_stream.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要后端实现。详见 AC-017~AC-019 |
| AC-013 | REQ-003 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | SSE 逐字输出角色回复。用户动作：前端发送消息；可观察结果：角色回复逐字输出。 | §2.3 + §2.6 `free_chat_service.send_message_stream()` 调用 `model_router.stream_with_fallback()` 获取 `AsyncGenerator[str, None]`，端点层逐 token 包装为 SSE `text` 事件 | DEV-001 | 待填写 | pytest backend/tests/integration/test_free_chat_stream_endpoint.py -v (backend/tests/integration/test_free_chat_stream_endpoint.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 Browser Interaction E2E；需要 API/DB/Runtime 契约验证 |
| AC-014 | REQ-003 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | SSE 流结束后在 done 事件中推送好感度变化。数据状态：好感度变化异步写入数据库。验证：SSE 流结束后查询好感度确认更新。 | §2.3 `done` 事件携带好感度变化；deferred task 异步写入 `Affection` 表 | DEV-001 | 待填写 | pytest backend/tests/integration/test_legacy_deferred_db_write.py -v (backend/tests/integration/test_legacy_deferred_db_write.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要 API/DB/Runtime 契约验证 |
| AC-015 | REQ-003 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 旧 POST /game/{session_id}/free-chat 保留兼容，不删除，标记 deprecated。验证：调用旧端点仍返回同步 JSON 200，功能不变。 | §7 旧端点保留兼容：`POST /game/{id}/free-chat` 不删除、不修改行为，响应头追加 `Deprecation: true` | DEV-001 | 待填写 | pytest backend/tests/integration/test_free_chat_stream_endpoint.py -v (backend/tests/integration/test_free_chat_stream_endpoint.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | CEO 附条件 C3 硬性约束。需要 API/DB/Runtime 契约验证 |
| AC-016 | REQ-003 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | FreeChatView.vue 改用流式端点，逐字显示回复。用户动作：用户打开自由对话界面发送消息；可观察结果：角色回复逐字显示。 | §3.2 `FreeChatView.vue` 的 `sendMessage()` 改为调用 `useSSEStream()` 指向 `/game/{id}/free-chat/stream`，`onText` callback 累积文本并更新 UI | DEV-002 | `frontend/src/views/FreeChatView.vue` sendMessage() uses `useSSEStream()` + `gameApi.getFreeChatStreamConfig()`; `frontend/src/api/game.ts` getFreeChatStreamConfig(); `tests/e2e/cr042-free-chat-stream.spec.ts` | npx playwright test frontend/tests/e2e/cr042-free-chat-stream.spec.ts --project=chromium --trace on (frontend/tests/e2e/cr042-free-chat-stream.spec.ts) | Verified | covered | DEV-002 实现完成，TypeScript 编译 0 错误 | 待 Browser E2E QA 验证 | 需要 Browser Interaction E2E。使用 useSSEStream composable（见 AC-020） |
| AC-017 | REQ-004 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 新增 stream_with_fallback(scenario, messages, **kwargs) -> AsyncGenerator[str, None] 方法。验证方法存在、返回 AsyncGenerator、逐 token yield。 | §2.4 `stream_with_fallback()` 方法签名 `AsyncGenerator[str, None]`，通过 `provider.stream_complete()` 逐 token yield | DEV-001 | 待填写 | pytest backend/tests/unit/test_model_router_stream.py -v (backend/tests/unit/test_model_router_stream.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要后端实现 |
| AC-018 | REQ-004 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 主模型流式调用失败时自动切换 fallback 模型继续流式输出，不中断用户体验。验证：mock 主模型失败，确认 fallback 模型被调用。 | §2.4 降级策略：主模型 `stream_complete()` 失败（timeout/connection error）→ `except Exception` 捕获 → 切换 fallback 链下一个模型继续 `stream_complete()` | DEV-001 | 待填写 | pytest backend/tests/unit/test_model_router_stream.py -v (backend/tests/unit/test_model_router_stream.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | 需要后端实现 |
| AC-019 | REQ-004 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 所有 fallback 模型均流式失败时返回 fallback 文本（非流式，一次性 yield）。验证：mock 所有模型失败，确认返回非流式 fallback 文本。 | §2.4 + §5 Q-003 所有 fallback 失败 → 一次性 yield `_get_stream_fallback_text(scenario)` 场景特定友好提示文本 | DEV-001 | 待填写 | pytest backend/tests/unit/test_model_router_stream.py -v (backend/tests/unit/test_model_router_stream.py) | Verified | covered | DEV-001 实现完成，35/35 自动化测试通过 | DEV-001 完成，待 PL 确认 | Q-003 已在 DESIGN 阶段确认：fallback 文本按场景区分，保持角色人设口吻 |
| AC-020 | REQ-005 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | 提取 useSSEStream() composable，封装 fetch + reader + SSE 解析。验证 composable 存在、可独立调用、正确解析 SSE 事件。 | §3.1 `composables/useSSEStream.ts` 新建，封装 `fetch` + `ReadableStream` reader + `data: ` 行解析 + 事件 dispatch（onText/onDone/onError/onEmotion/onAffectionUpdate/onGmUpdate） | DEV-002 | `frontend/src/composables/useSSEStream.ts` (144 lines); `tests/unit/fe/useSSEStream.test.ts` (18 test cases, 18/18 pass) | npx vitest run frontend/tests/unit/fe/useSSEStream.test.ts (frontend/tests/unit/fe/useSSEStream.test.ts) | Verified | covered | DEV-002 实现完成，18/18 单元测试通过 | DEV-002 完成 | 前端实现 |
| AC-021 | REQ-005 | P1 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | submitChoice（Legacy 分支）、submitCustomInput（Legacy 分支）、FreeChatView 均使用 useSSEStream() composable。验证三处代码均调用 composable。 | §3.2 三处代码调用 `useSSEStream()`：`submitChoice()` Legacy 分支、`submitCustomInput()` Legacy 分支、`FreeChatView.vue` sendMessage() | DEV-002 | `frontend/src/stores/game.ts` submitChoice Corvus branch uses `useSSEStream()` (line 552); `frontend/src/stores/game.ts` submitCustomInput uses `useSSEStream()` (line 661); `frontend/src/views/FreeChatView.vue` sendMessage uses `useSSEStream()` (line 241); Legacy submitChoice SSE branch uses `consumeSSEBody()` (same parsing logic, line 437) | npx vitest run frontend/tests/unit/fe/useSSEStream.test.ts (frontend/tests/unit/fe/useSSEStream.test.ts) | Verified | covered | DEV-002 实现完成，代码审查通过 | DEV-002 完成 | 需要 Browser Interaction E2E |
| AC-022 | REQ-005 | P0 | `openspec/changes/CR-042-prd-cr-040-非-corvus-路径-sse-流式改造/specs/capability/spec.md` | Corvus 分支迁移到 useSSEStream() composable 后功能不变。验证：Corvus 路径完整对话流程回归测试通过。用户动作：Corvus 对话全流程；可观察结果：对话逐字显示、好感度推送、元数据推送均正常。 | §3.3 Corvus 分支 `submitChoice()` 和 `submitCustomInput()` 中 inline SSE 解析逻辑替换为 `useSSEStream()` 调用，`onGmUpdate` callback 处理 `gm_update` 事件；迁移后运行 Corvus 回归 E2E | DEV-002 | `frontend/src/stores/game.ts` submitChoice Corvus branch migrated to `useSSEStream()` with `onGmUpdate` callback (line 552); submitCustomInput Corvus branch uses same composable (line 661); `tests/e2e/cr042-corvus-regression.spec.ts` | npx playwright test frontend/tests/e2e/cr042-corvus-regression.spec.ts --project=chromium --trace on (frontend/tests/e2e/cr042-corvus-regression.spec.ts) | Verified | covered | DEV-002 实现完成，TypeScript 编译 0 错误，gm_update callback 已迁移 | 待 Browser E2E QA 验证 | CEO 附条件 C2 — 若回归失败，Corvus 保持原逻辑，不阻塞本 CR。需要 Browser Interaction E2E |

## 状态规则

- `覆盖状态` 只允许使用 `covered`、`covered`、`manual_pending`、`deferred_with_approval`、`out_of_scope_with_reason`。
- P0/P1 验收项不得缺少 `覆盖状态`、`未覆盖原因` 或 `PL 处理`。
- 发布关口前，P0/P1 不能是 `covered`。
- 前端可见的数据展示、列表、详情、刷新和状态查询必须作为独立可测试 AC 或明确设计落点记录，不能只写创建/更新/删除。

## 验收项分类汇总

| 分类 | AC 编号 | 数量 | 优先级 | 下游测试标记 |
| --- | --- | --- | --- | --- |
| Legacy submit_choice SSE | AC-001 ~ AC-005 | 5 | P0: 3, P1: 2 | Browser E2E + API/DB/Runtime |
| Legacy submit_custom_input SSE | AC-006 ~ AC-010 | 5 | P0: 3, P1: 2 | Browser E2E + API/DB/Runtime |
| free-chat SSE | AC-011 ~ AC-016 | 6 | P0: 4, P1: 2 | Browser E2E + API/DB/Runtime |
| model_router 流式支持 | AC-017 ~ AC-019 | 3 | P0: 1, P1: 2 | API/DB/Runtime |
| 前端 SSE composable | AC-020 ~ AC-022 | 3 | P0: 2, P1: 1 | Browser E2E + 单元测试 |
| 合计 | — | 22 | P0: 13, P1: 9 | — |

## CEO 附条件覆盖映射

| 附条件 | 关联 AC | 验收方式 |
| --- | --- | --- |
| C1: PRD 技术细节不替代 REQUIREMENT/DESIGN 正式产出 | 全部 | PM 已独立确认需求；SA 在 DESIGN 阶段独立确认技术方案（§1~§7 + ADR-042-01~03） |
| C2: composable 提取后 Corvus 路径回归验证 | AC-022 | Browser Interaction E2E 回归测试；失败则 Corvus 保持原逻辑 |
| C3: 旧端点保留兼容（不删除）硬性约束 | AC-015, AC-002 | API 契约验证旧端点仍可用；preset/choice 保持 JSON |

## Open Questions

| 编号 | 问题 | 阻塞 MVP | 处理结论 | 展示状态 |
|---|---|---|---|---|
| Q-001 | SSE 流中断时的错误恢复行为是否需特殊处理？ | 否 | DESIGN 阶段已确认：复用 Corvus 路径已有模式（error 事件 + 关闭流 + 记录日志 + finally deferred DB 写入） | ✅ 已由 SA 在 DESIGN 阶段确认 |
| Q-002 | SSE 流式端点是否需要 heartbeat/timeout 机制？ | 否 | DESIGN 阶段已确认：复用 Corvus 路径已有模式（无 heartbeat，依赖 Provider stream timeout + Nginx proxy_read_timeout 300s） | ✅ 已由 SA 在 DESIGN 阶段确认 |
| Q-003 | AC-019 fallback 文本具体内容是什么？ | 否 | DESIGN 阶段已确认：按场景预设友好提示文本，保持角色人设口吻。FREE_CHAT: "（微微侧头，轻轻笑了笑）抱歉，我刚才走神了……你说的真有意思，能再和我说说吗？"；NARRATIVE: "（故事在这一刻仿佛停滞了片刻，随后又缓缓流淌……）"；默认: "抱歉，暂时无法回应，请稍后再试。" | ✅ 已由 SA 在 DESIGN 阶段确认 |

## 安全审查结论

| 项 | 内容 |
| --- | --- |
| 审查角色 | Security (Cat01-security) |
| 审查日期 | 2026-09-16T17:00+08:00 |
| 审查文件 | `workflow/changes/CR-042/security-review.md` |
| 结论 | **Passed** (含 3 项低风险建议，不阻塞) |
| 认证/授权 | ✅ 复用 Bearer Token + 所有权校验，SSE 端点认证完整 |
| 敏感数据 | ✅ SSE 流不输出 token/key/路径；日志无敏感数据 |
| Deferred DB | ✅ 独立 session，无竞态风险 |
| Mock 证据 | ✅ Delivery E2E + Browser E2E 均 Mock API=no |
| 低风险建议 | S-1 token 日志 hash (BE 后续迭代)；S-2 Nginx SSE 配置 (Ops deploy-plan)；H-1 StoryPanel v-html DOMPurify (FE 后续迭代) |
